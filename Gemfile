# fastlane 실행용 Ruby 의존성.
#
# iOS 릴리스는 macOS에서만 가능하다 (Windows 개발 머신에서는 `bundle exec fastlane ios ...`가
# 동작하지 않는다). 기본 실행 경로는 GitHub Actions의 macOS 러너이고,
# 절차 전체는 APP_STORE_RELEASE.md에 있다.
source "https://rubygems.org"

gem "fastlane"
