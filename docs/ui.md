# UI 명세 v0.3

> 자동 구현 시 AI가 임의 판단하지 않도록 화면·연출·카피 톤을 고정한다. ⚠ 표시는 **제안 상태 — 사용자 확정 필요** (확정되면 ⚠ 제거).
> 모바일 세로(390×844 기준) 우선, 데스크톱은 중앙 고정폭(최대 480px) 레이아웃.

- 공통 반응형 기준: 280~480px 화면 폭에서 의도하지 않은 가로 스크롤이나 잘린 카드·문구가 없어야 한다. 홈의 테마·추천 목록은 폭에 맞춰 열 수가 바뀌는 그리드를 사용하고, 긴 출발점·장소명은 컨테이너 안에서 말줄임 또는 최대 2줄로 제한한다.

## 디자인 톤

- ⚠ 컬러: 딥 네이비(#0F2540, 밤바다) 바탕 + 선셋 오렌지(#FF7A45) 포인트, 방위별 보조 색상 8종
- ⚠ 폰트: Pretendard (시스템 폴백)
- 무드: "게임"이되 유치하지 않게 — 나침반·항해 모티프. 소주병 이미지·일러스트는 사용하지 않는다 (확정)
- 카피 톤: 짧고 다정한 존댓말, 이모지 최소. 실패도 긍정 프레임 ("이 방향은 바다예요. 범위를 넓혔어요")

## 화면 흐름

```
S0 온보딩(첫 방문 1회) → S1 홈/스핀 ⇄ S2 출발점 선택(여행 모드)
                              │ 스핀 정지
                              ▼
                         S3 방위 연출 → S4 결과 카드 ┬→ S5 공유 카드
                              ▲______________│       └→ S6 방향 코스 (Phase 7)
                                             └────────── (다시 돌리기)
```

## 콜드 스타트 인트로

- 밝은 앱 배경에서 투명 배경의 브랜드 마크와 `Spindle` 워드마크가 순서대로 등장한다. 설치용 마스커블 아이콘의 배경·외곽은 인트로에 노출하지 않는다.
- 화면 하단에는 봉우리가 여러 번 굽이치는 Spindle 블루 2겹 물결이 낮게 찰랑이며 로딩 중임을 알린다. 약 2초 뒤 물결이 살짝 투명해지면서 760ms 동안 화면 전체를 한 번 채우고, 하단 공백 없이 홈 화면으로 480ms 디졸브한다.
- 화면을 탭하면 짧은 물결 상승과 함께 즉시 건너뛴다. `prefers-reduced-motion: reduce`에서는 찰랑임과 확대를 제거하고 짧은 페이드만 사용한다.

## S0 온보딩 (첫 방문 1회, 건너뛰기 가능)

- 2장: ① "휴대폰을 돌리면, 방향이 정해져요" (현장 모드 스핀 개념) ② "덜 알려진 부산을 만나요" (분산 서사 한 줄)
- 마지막에 모드 안내: 부산 현지면 현장 모드(위치·나침반 권한), 아니면 여행 모드
- 설정의 [사용법 다시 보기]는 이 슬라이드를 반복하지 않는다. 실제 홈 화면으로 이동한 뒤 출발점→스핀→테마→도장깨기 요소를 차례로 비추는 4단계 코치마크를 표시하며, 이전·다음·건너뛰기·완료를 제공한다. 안내 패널은 강조 대상의 화면 위치와 무관하게 모든 단계에서 하단 안전영역 위에 고정한다.

## S1 홈 = 스핀 화면 (핵심 화면)

- 2026-09-09 홈 폴리시: 헤더 → 해변 히어로/스핀 CTA → 조작 목적 한 줄 → 네 바로가기 → `발견해볼 부산` → 테마 → 도장 진행 → 하단 윙크 미션 순서로 구성한다. 아래 스핀 원판 설명은 스핀 탭에 적용한다.
- `발견해볼 부산`은 기존 네 곳의 고정 큐레이션이며 일일·개인화 추천으로 표시하지 않는다. 깡깡이 예술마을을 16:9 사진과 흰 본문 카드로 먼저 보여주고 기존 큐레이션 소개를 최대 두 줄 표시한다. 나머지 세 곳은 80px 썸네일·두 줄 이내 장소명·카테고리/지역의 가로 행이다. 카드 전체는 기존 상세 화면, `전체 명소`는 명소 탭으로 연결한다. 사진 위에 배지·텍스트를 겹치지 않으며 섹션 끝에 공공데이터 출처를 표시한다.
- 홈 전용 CSS Module에서 정보 위계를 정돈한다. 360px 이상 테마는 2+3, 더 좁은 화면에서는 기존 2+2+1 배열을 유지한다. 사진 지연 로딩·실패 폴백, 비로그인·좌표 무전송, 추천 엔진은 그대로 유지한다.
- 좌측 상단 브랜드 로고는 설치 아이콘에서 흰 배경을 제거한 파란 별·궤도 심볼을 사용해 홈 화면·PWA 아이덴티티를 통일한다.
- 브랜드 마크·설치용 앱 아이콘과 기존 테마 카드의 색·구성은 유지한다. 홈 히어로만 수면 위에서 바라본 얕은 청록 바다·흰 포말·모래사장·산과 방파제 실루엣으로 구성해 부산 해안가 무드를 더하며, 왼쪽 카피와 오른쪽 별이 마스코트의 가독성을 보존한다.
- 홈의 `명소·도장깨기·여행모드·축제` 바로가기는 각각 파도 핀·별을 품은 조개 도장·해안 등대·바다 위 불꽃을 사용한다. 네 아이콘은 기존 블루 계열 안에서 청록·모래색을 보조로 쓰는 반투명 해변 유리 타일로 통일한다.
- 하단 내비게이션은 안전영역을 제외한 바 높이를 80px 이내로 유지한다. 가운데 스핀 버튼만 위로 돌출시키고 나머지 탭과 레이아웃 높이를 맞춰 홈 콘텐츠를 과도하게 가리지 않는다. 모든 탭은 44px 이상 터치 영역을 유지한다.
- 중앙: **나침반 로즈(compass rose) 원판** — 8방위 눈금. 현장 모드에서는 기기 방위각을 따라 회전하고, 여행 모드에서는 원판을 직접 휙 돌려 관성 스핀
- 원판 중앙은 캐릭터 이미지 대신 북쪽을 가리키는 양면 나침반 바늘과 작은 브랜드 별 허브를 사용한다. 캐릭터가 방위 눈금의 기능성을 가리지 않게 한다.
- 상단: 출발점 표시 — 여행 모드는 나침반 현장 모드 진입 아이콘과 `{출발점} 기준` 버튼(탭 → S2), 현장 모드는 탭해서 여행 모드로 돌아오는 `내 위치 기준 · 나침반` 상태 버튼. 별도 모드 배지는 두지 않는다(조용한 텍스트 줄로 녹인다).
- 화면 하단: **이동시간 다이얼**을 내비게이션 위에 유지한다. 요약은 `이동시간 한도`와 실제 선택값 `최대 40분`으로 구분한다. 20분부터 480분까지 1분 단위로 조절하며 마지막 값은 `제한 없음`(Infinity)이다. 분위기 라벨과 `하루`는 슬라이더에 표시하지 않는다. 하단 앵커는 20분·1시간 30분·제한 없음이며 현재 값처럼 강조하지 않는다. 기존 앵커 보간·화살표 ±1분·PageUp/Down ±30분·Home/End 동작은 유지한다.
- 다이얼 아래: 테마 스핀 여행 상태. 미선택이면 `테마 없이`, 선택하면 테마명·현재 장면/전체 장면·[바꾸기]·[해제]를 표시한다. 디스크 광원과 하단 테마 표식은 선택 테마 색을 사용하되 8방위 눈금과 나침반 바늘은 유지한다.
- 현장 모드 스핀: 손가락 드래그가 아니라 **휴대폰을 실제로 돌리는 속도**에 맞춰 원판이 회전한다. 기기 회전이 멈추고 방위값이 안정되면 실제 방위각을 잠가 8방위로 분류한다. 화면만 추가로 도는 가짜 관성은 사용하지 않는다.
- 여행 모드 스핀: 별도 `돌리기` 버튼 없이 원판을 직접 드래그하고 휙 놓아 스핀한다. 손가락 속도에 따라 관성 회전한 뒤 멈춘 원판의 방위각을 결과 입력으로 그대로 사용한다. 키보드 Enter·Space는 동일 원판 조작의 대체 입력이다. 원판의 8방위 라벨은 회전 중에도 역회전해 항상 똑바로 읽히며, 원판 접근성 설명은 화면의 조작 안내 문구와 연결한다.
- 흔들기 스핀(여행 모드): 휴대폰을 흔들면 흔드는 동안 원판이 계속 돌고, 세게·오래 흔들수록 빨라진다. 흔들기를 멈추면 손가락 플릭과 같은 관성 감속으로 서서히 멈춰 그 방위각을 결과 입력으로 쓴다. 걷는 진동으로 멋대로 돌지 않도록 분명한 흔들기에만 반응하고, 손가락 드래그가 시작되면 흔들기 입력은 즉시 양보한다. 안내 문구는 흔들기가 켜졌을 때만 `휴대폰을 흔들거나, 원판을 휙 돌려보세요`로 바뀐다.
- 흔들기 권한: **전용 버튼을 두지 않는다.** 안드로이드처럼 권한 개념이 없는 기기는 스핀 화면 진입 시 바로 켜고, iOS는 스핀 화면에서 일어나는 첫 조작(탭·드래그를 놓는 순간·키 입력)에 권한 요청을 얹는다 — 조작 자체를 막지 않으므로 첫 드래그 스핀은 그대로 완주되고 손을 뗀 뒤 프롬프트가 뜬다. 화면을 떠나는 하단 내비게이션 탭에는 얹지 않으며, 같은 페이지 로드에서 이미 답을 받았으면 다시 묻지 않는다. 페이지 로드 즉시 요청하는 자동 프롬프트는 여전히 금지(iOS가 조용히 거부한다). 거부·미지원이면 사유 한 줄을 보여주고 드래그 스핀으로 계속하게 한다(흔들기는 어디까지나 보조 입력이며, 없어도 전 동선이 동작해야 한다).
- 현장 모드 첫 진입: 상단 나침반 아이콘 버튼에서 권한을 요청한다 — 자동 프롬프트 금지, 거부 시 여행 모드 전환 안내
- 현장 모드 중에는 별도 하단 CTA를 두지 않는다. 겨눈 채 방위가 안정되면 자동으로 잠기되, 켜자마자 잠기지 않도록 약 2.5초 조준 유예를 둔다. 상단 `내 위치 기준 · 나침반` 상태 버튼을 누르면 여행 모드로 돌아온다.
- 나침반 권한 거부·미지원, 위치 권한 거부·취득 실패, 부산 원도심·영도 권역 밖은 모두 사유 한 줄을 보여주고 여행 모드로 계속하게 한다. 현장 모드에서는 현재 위치가 추천·코스의 출발점이 된다.
- 홈 마지막에 윙크 별이와 `오늘의 미션` 안내를 둔다. 단말 로컬 날짜에 따른 제목과 설명을 표시하고 전체를 누르면 기존 스핀 화면으로 이동한다. 미션 완료 상태는 표시·저장하지 않으며 추천 점수나 테마를 변경하지 않는다.
- 미션 안내는 연한 블루 배경과 16px 둥근 모서리를 사용하고 44px 이상 터치 영역을 확보한다. 280~480px에서 제목과 설명이 겹치지 않도록 줄바꿈을 허용한다.

## S2 출발점 선택 (여행 모드)

- 프리셋 카드 3개: 부산역 / 남포동 / 영도(흰여울 입구) — 탭 1번으로 완료
- 하단: `지도에서 좌표 직접 찍기` → S2b. 지도에서 찍은 지점은 프리셋 아래 한 칸으로 남아 다시 고를 수 있다.
- 선택 즉시 S1 복귀

## S2b 지도에서 출발점 고르기 (여행 모드)

- 지도를 끌어 화면 중앙 십자 핀에 출발 지점을 맞춘다. 별도 핀 탭 없이 중앙이 곧 후보 좌표다.
- 하단 시트 한 줄에 후보 지점의 존 이름(`남포·광복 근처`)을 조용히 표시하고, `[여기서 출발하기]`로 확정한다.
- 4개 구 권역 밖이면 경계로 스냅하지 않는다. `권역 밖이에요` 한 줄과 함께 확정 버튼을 비활성화해 사용자가 직접 옮기게 한다 (스냅은 사용자가 찍지 않은 지점을 출발점으로 만든다).
- 우하단 `내 위치로 이동` 버튼은 GPS 1회 취득으로 지도만 옮기고, 카카오맵·내장 지도 모두 골목 단위 확대 수준으로 맞춘다. 권한 거부·실패는 사유 한 줄을 보여주고 지도 조작으로 계속하게 한다. 취득 좌표는 단말 밖으로 나가지 않는다.
- 카카오맵을 못 불러오면 내장 벡터 지도(`LocalMapView`)에서 동일하게 동작한다.

## S3 방위 연출 (스핀 정지 직후, ~2초)

- 정지한 방위 부채꼴 하이라이트 + 방위명 + **8방위 큐레이션 메시지** 1줄
- 목적지 공개 전 카드는 관광 패스 형태로 구성하고, 사진 미리보기·별이·"탭해서 먼저 보기"·자동 공개 진행선을 표시한다. 큰 물음표만 둔 빈 카드는 사용하지 않는다.
- 공개 카드 위에는 별도의 "○쪽이에요" 배지를 반복하지 않는다. 추천 장소 사진을 카드 중앙에 미리 보여주고, 신난 별이는 사진 우측 하단에 겹쳐 배치하며 바로 위 말풍선에 "여기 어때요?"를 표시한다.
- 인접 확장 발생 시 메시지 하단에 사유 1줄
- 자동으로 S4 전환 (탭으로 즉시 스킵 가능)

### 8방위 메시지 (Phase 5에서 확정, 예시 톤)

동: "시작과 새벽의 방향. 떠오르는 해를 만나러 가요" — 나머지 7종 동일 톤·길이(공백 포함 25자 내외)로 작성

## S4 결과 카드

위에서 아래로:
1. 대표 이미지 (없으면 방위 색상 배경 + 나침반 아이콘 폴백) — 높이는 `clamp(190px, 30svh, 300px)`. 배지·오버레이 칩은 올리지 않는다 (우상단 [사진] 제외). 이미지 탭 = 갤러리.
2. 관광지명 + 보조줄 "카테고리 · 구 · 도보 약 n분" + 주소 한 줄.
3. **방문 정보**: 주소 바로 아래에 온다 — 갈 수 있는 곳인지가 설명보다 먼저다. 제목 행 없이 라벨–값 표만 두고(화면에서 뺀 제목은 `aria-label`로 남긴다), `detailIntro2`의 유형별 원문 중 값이 있는 항목을 중요도순 최대 4행 표시한다. 관광지=이용시간·휴무일·체험 안내·주차, 문화시설=관람시간·휴무일·이용요금·관람 소요시간, 레포츠=이용시간·휴무일·이용요금·예약 안내, 쇼핑=영업시간·휴무일·주요 품목·장날, 음식점=영업시간·휴무일·대표 메뉴·예약 안내. 빈 항목은 후순위 값으로 보충하고, 배지·상태 아이콘·색상 칩 없이 구분선과 라벨–값만 사용한다.
4. **이곳의 매력**: 방문 정보와 마찬가지로 제목 행 없이 소개문과 [자세히 보기]만 둔다(제목은 `aria-label`로 남긴다). `detailCommon2.overview` 앞 문장 최대 3개·160자, 3~4줄 프리뷰. 전체 원문은 [자세히 보기]에서 제공.
5. **위치 지도**: 설명 다음에 둔다. 길찾기는 하단 액션 바에 고정돼 있어 카드 안 지도는 확인용이며, 방문 정보와 설명 사이를 끊지 않도록 뒤로 뺀다.
6. [자세히 보기] 시트는 주소·이동과 정규화된 전체 방문정보를 설명보다 먼저 배치한다. TourAPI 원문의 `<br>`은 실제 줄바꿈으로 정리한다.
7. 버튼: **[길찾기]** (카카오맵. 누른 뒤에만 "카카오맵이 열리지 않았나요? 모바일 웹으로 열기" 한 줄이 조용히 붙는다) / [다른 후보] (나머지 2개 순환) / [다시 돌리기] (S1)
8. 코스 진입: 장식 아이콘 상자 없는 텍스트 중심 행 "이 방향으로 코스 짜기 — 가까운 장소 2~4곳을 이어 코스로 만들어요" → S6. 실패 사유도 아이콘·색상 박스 없이 일반 보조 문장으로 표시한다.

- 인기도 티어는 추천 가중치에만 사용하고 결과 화면에 "숨은 명소" 배지로 표시하지 않는다.
- 축제 안내는 이모지·그라데이션 상태 배너 대신 행사명·기간·지역을 담은 편집형 안내 영역으로 표시한다.
- 길찾기에서 새 도장을 얻으면 별도 토스트로 버튼을 가리지 않고, 길찾기 시트 안에 "○○구 여행 도장을 받았어요" 인라인 안내를 표시한다.

- 로딩: 스켈레톤 카드. 에러: **원인 한 줄 + [다시 시도]** (빈 화면 금지). 원인 문구는 `연결이 끊겨 있어요`·`응답이 늦어지고 있어요`·`서버가 잠시 불안정해요`처럼 사용자가 대응할 수 있는 말로 쓰고, `resultMsg`·HTTP 상태 같은 기술 문자열은 콘솔에만 남긴다.
- 테마 스핀 결과는 설명 영역에 `테마 여정 n/N`과 랜덤 미션 1개를 텍스트 중심 영역으로 표시한다. 마지막 장면 전에는 [같은 테마로 다음 스핀], 마지막에는 [테마 여정 마치기]를 제공하며 기존 하단 다시 돌리기와 중복 노출하지 않는다.

## 테마 덱과 테마 스핀 (Phase 7)

- 테마 덱은 바다/골목·시장/근현대·역사/야간/먹거리 칩, 선택 테마의 사진 히어로 카드, 후보 수·예상 장면 수, [이 테마로 돌리기]를 첫 화면에 배치한다.
- 사진 히어로 카드는 테마별 대표 장소의 TourAPI 이미지를 화면 진입 시 실시간으로 불러오고, 이미지가 없거나 호출에 실패하면 테마 색과 상징을 조합한 배경을 유지한다. 응답을 영속 저장하지 않는다.
- 카드 배경은 테마 전환을 알리는 완만한 색 레이어 이동과 사진 줌인만 사용한다. `prefers-reduced-motion: reduce`에서는 모든 반복·줌 모션을 제거한다.
- 관광지 그리드는 `이 테마에 들어 있는 장소` 미리보기로 유지한다. 장소 카드를 누르면 기존 상세로 이동하지만 기본 CTA는 테마 스핀이다.
- 테마 칩 아래의 선택 테마 설명·사진 히어로 카드·관광지 그리드는 하나의 세로 스크롤 흐름으로 묶는다. 히어로 카드를 별도 고정 레이어로 남기지 않으며 스크롤하면 장소 목록과 함께 위로 이동한다.
- 실제 스핀 디스크는 관광지명을 미리 쓰지 않고 선택 테마의 색·이름만 반영한다. 방향이 정지한 뒤에만 관광지를 공개한다.
- 테마 후보가 0개인 빈 상태에서는 CTA를 비활성화하고 다른 테마 선택을 안내한다. 모든 버튼은 44px 이상이며 280~480px에서 히어로 카드·CTA·긴 테마명이 잘리지 않아야 한다.

## S5 공유 카드

- 1080×1920(인스타 스토리) PNG: 현재 밝은 블루 테마의 흰 여행 엽서. 현행 `brand-mark-192.png`와 워드마크, 장소명·지역·도보 시간, 큰 대표 사진, 메시지, 푸터 순으로 배치한다. 사진 아래 `○쪽으로 만난 부산` 방위 배지는 두지 않는다. 옛 밤바다 그라디언트·4각별 로고는 사용하지 않는다.
- 미리보기와 저장·공유는 같은 PNG를 사용한다. 사진 없음·로드 실패 시 밝은 바다와 나침반 일러스트로 대체하고 긴 장소명은 글자 크기와 최대 3줄로 조정한다. 생성 중 버튼 비활성화, 생성 실패 시 재시도를 제공한다.
- 푸터에 `출처: ⓒ한국관광공사`를 텍스트로 표기한다.
- 버튼: [공유하기] (Web Share API) / [저장] (다운로드 폴백)
- 공식 CI/BI 로고 이미지와 공사가 직접 개발·운영하는 서비스로 오인될 브랜딩은 사용하지 않는다 (절대 원칙 6).

## S6 방향 코스 (Phase 7)

- 상단: 확정 방위 + 출발점 + 이동시간 다이얼 요약. 스핀 결과를 바꾸는 별도 방위 선택은 제공하지 않는다.
- 본문: 방문 순서대로 2~4개 장소를 세로 목록으로 표시. 각 항목은 순번·대표 이미지·관광지명·이전 지점에서의 보정 이동시간·운영상태·[길찾기]를 포함한다.
- 하단: 전체 보정 이동시간과 "예상 이동시간이며 실제 경로와 다를 수 있어요" 안내, [다시 돌리기] 제공.
- **카카오맵 CTA**: 두 링크를 나란히 늘어놓지 않고 하단 기본 CTA [코스 안내 시작]으로 단계 카드를 연다 (S7 참고). 사용자 현재 위치는 어느 링크에도 넣지 않는다. 1번 좌표가 유효하지 않으면 1단계 링크를 숨기되 2단계로 건너뛰지 않고, 전체 코스의 유효 장소가 2곳 미만이면 2단계 링크도 숨겨 장소별 [길찾기]만 남긴다. 카피는 `내비 시작`처럼 앱 실행·턴바이턴을 보장하는 표현을 쓰지 않는다.
- 인접 방위 확장 또는 목표 장소 수 축소가 발생하면 목록 위에 사유를 한 줄로 표시한다.
- 후보가 2개 미만이면 S4를 유지하고 "이 방향에서는 코스를 만들 장소가 부족해요"를 표시한다.
- 로딩은 코스 목록 스켈레톤, 일부 상세 호출 실패는 해당 장소를 제외하고 재구성, 재구성 후 2개 미만이면 S4 폴백.

## S7 코스 안내 단계 (Phase 7)

> 별도 안내 화면은 없다. 자체 경로안내를 폐기하고(docs/course.md §8) S6 안에서 단계만 끊는다.

- S1 스핀 상단에 `한 곳 / 코스` 세그먼트를 둔다. 코스 선택 시 제목은 `한 번 돌려 오늘의 코스를 만들어요`로 바꾸고, 진행 중 테마 여정이 있으면 `코스 모드에서는 테마 여정을 잠시 마쳐요`를 한 번 표시한다.
- S6 하단 기본 CTA는 `코스 안내 시작` 버튼이다. 누르면 화면 이동 없이 카드 스트립 아래에 단계 카드가 열리고, 하단 CTA는 `안내 종료`로 바뀐다. 장소별 `카카오맵 길찾기`는 항상 보조 CTA로 남긴다.
- **1단계**: `1단계 · {1번 장소명}로 이동` 제목, `카카오맵에서 1번 장소까지 안내받으세요. 출발지는 카카오맵이 직접 잡습니다.` 안내, 기본 버튼 `카카오맵으로 길찾기`, 보조 버튼 `도착했어요 · 다음 단계로`.
- **2단계**: `2단계 · 코스 순서대로 이동` 제목, `이제 N곳을 순서대로 둘러보세요. 카카오맵이 1번부터의 방문 순서를 이어서 안내합니다.` 안내, 기본 버튼 `카카오맵에서 전체 코스 보기`, 보조 버튼 `1단계로 돌아가기`. 2단계로 넘어가면 카드 스트립·지도 선택도 2번 장소로 옮긴다.
- 단계 전환은 사용자 확인 동작으로만 일어난다. **GPS·나침반 권한을 요청하지 않고 위치를 구독하지 않는다.** `현재 위치 추적 중`처럼 추적을 시사하는 표현도 쓰지 않는다.
- 좌표가 없어 링크를 만들지 못하면 버튼을 숨기고 사유를 한 줄로 알린다 — 1단계는 `아래 카드의 장소명으로 검색해 주세요`, 2단계는 `장소별로 길찾기를 이용해 주세요`.
- 단계 카드는 `aria-live="polite"`로 단계 변경을 읽어 준다. 최소 터치 영역은 44px을 유지한다.

## 명소 둘러보기 혼잡 예측 (Phase 7)

- 명소 탐색 필터는 전체·음식점·카페 밑줄 탭과 모든 지역 드롭다운을 한 줄로 제공한다. 지역은 블루 테마의 둥근 팝오버로 고르며 선택 지역에 체크 표시를 제공한다. 바깥 클릭·Escape·뒤로가기로 닫히고 종류와 AND로 결합한다. 음식점·카페에서는 혼잡 요약(로딩·오류·빈 상태 포함), 지도 핀·선택 카드·리스트의 혼잡 배지를 숨긴다. 장소 데이터 로딩·실패·빈 상태와 운영 중단 안내는 유지한다.

- 카카오맵 JS SDK 베이스맵 위에 자체 `CustomOverlay` 핀과 하단 카드 스트립을 표시한다. 키 누락·SDK 로드 실패 시 기존 자체 벡터 지도로 자동 폴백하며, 어느 경우에도 기본 정보 구조는 유지한다. 당일 예상 집중률이 높은 명소 핀에는 선셋 오렌지 원형 `!`와 `오늘 혼잡 예상`, 낮은 명소 핀에는 초록 원형 `✓`와 `오늘 가기 좋아요`를 표시한다. 지도 왼쪽 위 범례에도 두 상태를 텍스트로 함께 제공한다. 두 상태가 모두 0건이면 `오늘 표시할 혼잡 예측 없음`으로 빈 상태를 설명한다.
- 명소 지도 핀은 큐레이션 POI와 전체 명소 모두 같은 크기의 물방울형으로 통일하고 44px 이상 터치 영역을 확보한다. 일반 핀 색은 저채도 Spindle 블루 하나로 고정하고, 큐레이션은 중앙 별·전체 명소는 중앙 점으로 구분한다. 선택해도 크기를 바꾸지 않고 주색·흰 키라인·그림자·라벨·레이어 순서로만 강조한다. 카카오맵 레벨 7 이상(내장 지도 z<2)은 티어 1 외 핀을 화면상 48px 거리로, 레벨 5~6(내장 지도 2≤z<3.4)은 전체 명소만 40px 거리로 묶고 더 확대하면 전부 개별 표시한다. 같은 id·좌표의 묶음 오버레이는 팬 이동 뒤에도 재사용해 깜빡임을 막는다. 선택 핀은 묶음에서 빠지며 묶음 핀을 누르면 중심으로 이동해 한 단계 확대한다. 라벨은 선택했거나 지도를 확대했을 때만 노출한다. 큐레이션 POI와 `contentId`가 겹치는 항목은 전체 명소 핀에서 제외한다. 코스 지도의 순번 핀은 숫자 가독성을 위해 별도 크기를 유지한다.
- 명소 지도 출처 `출처: ⓒ한국관광공사`는 별도 푸터 행을 만들지 않고 지도 왼쪽 하단에 작은 플로팅 오버레이로 표시한다. 하단 내비게이션과 지도 제공자 표기를 가리지 않으며 지도는 네비게이션 뒤까지 빈틈없이 이어진다.
- 전체 명소 축소형 핀을 탭하면 지도 위에 하나의 플로팅 카드만 표시한다. 카드에는 이름·카테고리·구·`○쪽 도보 약 ○분`을 담고, [자세히 보기]는 기존 상세 화면으로 연결한다. 방위와 도보 분은 출발점 기준으로 단말에서 계산한다.
- 전체 명소 데이터 호출에 실패하면 축소형 핀은 미표시하되 **조용히 넘어가지 않는다.** 성공 시 개수를 알려주던 같은 자리(지도 좌상단 상태 줄)에 `관광공사 등록 명소를 더 불러오지 못했어요 · {원인}`과 [다시 시도]를 같은 글줄 형식으로 표시한다. 큐레이션 지도는 그대로 유지되며, 재시도는 실패한 구만 다시 호출한다. 리스트 모드는 이번 범위에서 변경하지 않고 큐레이션 명소만 표시한다.
- 같은 상태를 하단 카드와 리스트에도 `오늘 혼잡 예상` 또는 `오늘 가기 좋아요` 텍스트로 제공해 색·아이콘만으로 의미를 전달하지 않는다. 해당 핀의 접근성 이름에도 같은 문구를 포함한다.
- ⚠ 표시 기준은 당일 예상 집중률 `70/100 이상`을 혼잡 예상, 당일 최고 예측값 `40/100 이하`를 가기 좋음으로 한다. 둘 다 공식 혼잡 등급이 아닌 Spindle의 보수적 제안값이며 실데이터 확인 후 조정할 수 있다. 중간값과 데이터가 없는 장소에는 상태를 표시하지 않는다.
- 예측은 정보 표시 전용이다. 혼잡한 명소도 무작위 추천 후보에서 제거하거나 점수를 낮추지 않는다.
- 로딩은 지도 위의 작은 `혼잡 예측 확인 중` 상태로 표시한다. 실패하면 `혼잡 예측을 불러오지 못했어요`와 44px 이상 `다시 시도` 버튼을 표시하되 지도·필터·명소 카드는 계속 사용할 수 있어야 한다.
- 관광지 집중률은 실시간 현장 인원수가 아닌 예측값이므로 `현재 혼잡`, `현재 한산`, `실시간 혼잡` 표현을 사용하지 않는다.

## 축제 화면 (홈 바로가기 · `지금 원도심 축제`)

- 기본은 오늘 진행 중인 축제 목록(종료 임박 순), 부제는 `오늘 진행 중인 축제만 모았어요`.
- 진행 중이 0건이면 같은 `searchFestival2` 응답 안의 **예정 축제**로 대체한다(시작 빠른 순). 부제는 `진행 중인 축제가 없어 곧 열릴 축제를 모았어요`, 각 카드 날짜 줄 앞에 `곧 열려요 ·`를 붙인다. 추가 API 호출은 하지 않는다.
- 진행 중·예정이 모두 0건일 때만 빈 상태(`지금은 진행 중인 축제가 없어요` + [스핀하러 가기])를 유지한다. TourAPI 축제 등록이 비어 있는 기간이 실제로 존재하므로 빈 상태를 없애지 않는다.

## 공통 규칙

- 획득 도장은 파란 선화의 "참 잘했어요" 별이 도장 이미지를 사용하며, 흰색·체크무늬 배경 없이 투명하게 표시한다.
- 로딩·에러·빈 상태 없는 화면 금지 (submission-check 항목)
- 터치 타겟 44px 이상, 세로 스크롤 1화면 이내 (S4·S6 제외)
- 모션은 기능 상태를 **토스식 스프링 감각으로 또렷하게** 전달하되(의미 없는 장식 모션은 지양), 화면 전환은 좌우 슬라이드 없이 **깊이(z축) 큐**로 위계를 표현한다. 탭 간 이동은 잔잔한 디졸브, 더 깊은 화면 진입(forward)은 새 화면이 살짝 확대되며 안착하는 zoom-in, 상위 복귀(back)는 살짝 축소되며 안착하는 zoom-out으로 처리한다(모두 가로 이동 없음). 스핀→공개→결과만 페이드·확대와 팝을 사용하며 하단 내비게이션은 고정한다.
- 버튼은 역할별 눌림 피드백을 제공한다. 누르는 순간은 70ms 이내로 빠르게 반응하고 Chrome의 빠른 클릭에서도 press 상태를 최소 110ms 보장한 뒤, 손을 떼면 약 440ms의 **또렷한 스프링 곡선(1.0을 약 6% 지나쳤다 정착)**으로 복원한다. 일반 버튼은 미세 축소, 주요 CTA·스핀 버튼은 더 깊은 축소·하강(scale 0.94)으로 쫀득하게, 카드형 버튼은 밝기와 깊이를 함께 조절한다. 시트·다이얼로그·상태 안내는 짧은 등장만 허용한다.
- **히어로 순간**(리빌 카드·별이 마스코트·결과 카드·도장 획득 안내·하단 탭 선택)은 등장 시 살짝 지나쳤다 정착하는 팝(spring-pop)으로 "즐거운 순간"을 만든다. 마스코트 팝인·도장 찍힘은 각 1회만 재생하고 무한 반복하지 않는다.
- 새 카드와 후보·테마·존 변경 결과는 스프링 팝의 상승·페이드로 나타내고, 카드 목록은 항목당 45ms 간격으로 최대 225ms까지만 순차 등장한다. 관광지 사진은 로드 완료 시 420ms 디졸브로 폴백 위에 표시한다.
- 지원 브라우저에서는 View Transition API로 이전·다음 화면을 320~460ms 동안 겹쳐 전환한다(이전 화면 고정 + 새 화면이 깊이 큐로 페이드인). 미지원 브라우저는 동일한 진입 모션으로 폴백한다.
- `prefers-reduced-motion: reduce`에서는 화면 이동·확대·팝·무한 반복과 코치마크 자동 스크롤을 즉시 전환으로 대체한다.

### 실패 안내

- 실패 문구는 **무엇을 못 불러왔는지 + 원인**을 한 줄로 잇는다 (`혼잡 예측을 불러오지 못했어요 · 네트워크 연결이 끊겨 있어요`). 앞부분은 화면이 쓰고 뒷부분은 공통 함수가 만든다.
- 원인을 특정할 수 없으면 기존 문구 `잠시 연결이 어려워요`로 떨어진다. 문장 끝에 마침표를 찍지 않는다.
- 연결 상태 상시 배지·상태 점은 두지 않는다. `navigator.onLine`은 `false`일 때만 신뢰하므로 상시 표시는 틀린 만큼 자주 맞다.
- 재시도는 세션 메모리 캐시를 비우고 다시 호출하는 것까지다. 실패한 요청을 쌓아 뒀다 나중에 보내는 동작은 만들지 않는다 (AGENTS.md 절대 원칙 3).

### 하드웨어 뒤로가기 (안드로이드 앱 전용)

> 웹(PWA·브라우저)에는 적용하지 않는다. 심사 제출 URL의 동선을 바꾸지 않기 위해 `history.pushState`를 도입하지 않는다.

우선순위대로 처리한다.

1. **열린 오버레이·내부 단계부터 되돌린다** — 결과 카드의 갤러리·[자세히 보기] 시트, 명소 지도의 핀 시트(지도 모드에서만), 코스 안내 단계 카드, 설정의 개인정보처리방침 시트, 홈 코치마크, 온보딩 슬라이드. 가장 나중에 연 것부터 닫히며 화면은 그대로 있다. 보이지 않는 상태를 닫느라 뒤로가기가 조용히 먹히면 안 되므로, 가드는 **실제로 렌더되고 있을 때만** 건다.
2. **화면 스택을 거슬러 이동한다** — 목적지는 **그 화면의 ← 버튼과 항상 같다.** 탭 화면(명소·스핀·도장·설정)은 이력을 쌓지 않고 홈으로 모이고, 리빌 연출에서는 스핀으로 되돌아간다.
3. **홈·온보딩 첫 슬라이드에서는 두 번 눌러 종료한다** — 첫 번째는 `뒤로 한 번 더 누르면 앱을 닫아요`(⚠ 제안) 안내만 띄우고, 2초 안에 한 번 더 누르면 종료한다. 2초가 지나면 안내부터 다시 시작한다.

안내는 알약·색 배경·아이콘 없이 하단 내비게이션 위에 뜨는 조용한 한 줄이며 `role="status"`로 읽힌다. 어떤 화면 이동이 일어나도 즉시 걷힌다.

## 2026-09-07 디자인 폴리시

- 후속 수정: 오늘의 미션은 홈 맨 아래(오늘의 스핀 추천 다음)에 윙크하는 별이와 함께 둔다. 연한 블루 그라디언트·24px 모서리·기존 타이포를 적용하고 누르면 스핀으로 이동한다.
- 결과 카드 주소 아래 `여기에 있어요` 지도 카드를 표시한다. 카카오맵에 출발점과 선택 장소를 함께 맞추고 방향·직선거리·기준점을 텍스트로 제공한다. 기준점 문구는 추천에 쓴 출발점의 이름을 그대로 쓴다 — 현장 모드는 `내 위치 기준`, 여행 모드는 `{출발지} 기준`. 새 위치 권한 요청이나 추적은 하지 않는다. 점선은 방향 표시임을 설명하고 실제 길안내는 기존 길찾기를 사용한다. 지도 로딩·SDK 실패 시 기본 지도와 재시도를, 좌표가 없으면 목적지/기준점을 구분한 안내를 제공한다. 후보 전환 시 지도와 거리도 함께 바뀐다.
- 이 작은 지도는 **탭해야 조작이 열리는 2단계 제스처**를 쓴다. 잠긴 상태(기본)에서는 드래그·휠·핀치를 받지 않아 지도 위에서 손가락을 위아래로 쓸면 결과 카드가 그대로 스크롤된다(`touch-action: pan-y`). 지도 아래쪽에 `지도를 탭하면 움직일 수 있어요` 알약을 띄우고, 지도를 탭하면 드래그·줌이 살아난다. 열린 뒤에는 왼쪽 아래 `지도 잠그기`로 되돌리며, 캡션도 잠그면 화면을 넘길 수 있다는 안내로 바뀐다. 다른 후보로 넘어가면 자동으로 다시 잠근다. 출처 표기는 결과 화면 하단의 기존 한 줄을 그대로 쓰고 지도 카드에 중복 표기하지 않는다.

### 음식점·카페 스핀

- 스핀 상단의 한 곳/코스 전환을 제거하고 관광지·음식점·카페 3열 세그먼트를 배치한다. 관광지는 기존 전체 추천을 사용한다. 선택 상태는 다시 돌리기와 탭 이동 후에도 유지하며 테마 시작 시 관광지로 초기화한다. 코스 구성은 관광지 결과의 기존 CTA에서 이어간다.
- 음식점 제목은 `오늘, 어디서 먹을까요?`, 카페 제목은 `잠깐, 커피 한 잔 할까요?`. 원판 색·조작·이동시간 슬라이더는 공통으로 유지한다. 테마 여정에서는 종류 탭 대신 적용 중 테마와 해제를 표시한다.
- 목록 준비 중과 오류 시 원판을 잠그고 로딩 또는 재시도를 안내한다. 빈 결과에서는 원판을 다시 사용할 수 있게 하고 방향·이동시간·종류 변경을 안내한다. 원판 회전 중 종류 전환은 잠근다.
- 결과 후보는 선택 종류 안에서만 순환하며 `다른 음식점 보기`/`다른 카페 보기`를 표시한다. 종류별 결과에서는 코스 생성 CTA를 숨긴다.

- 하단 스핀 아이콘 최종 조정: 기존 앱 브랜드 자산 `brand-mark-192.png`를 32px로 사용한다. 48px 흰색 원형 바탕과 옅은 테두리, 선택 시 얇은 블루 테두리와 연한 외곽 강조를 적용한다. 아래 검토본의 별·나침반 심볼과 선택 시 단색 채움은 이 조정으로 대체한다.

- 하단 내비게이션 검토본: 5개 동일 폭 열과 같은 라벨 기준선을 유지한다. 일반 선택 탭은 연한 블루 캡슐, 중앙 스핀은 48px 별·나침반 심볼로 상단 약 8px만 돌출한다. 비선택 스핀은 연한 블루, 선택 시 주색으로 표시한다. 모든 버튼은 44px 이상 터치 영역을 제공하며 배경 전환은 150ms, 모션 감소 시 기존 전역 규칙에 따라 사실상 즉시 반영한다. 흰 바탕·하단 안전영역은 유지한다.

- 홈 도장 현황은 수치와 작은 진행 막대로 압축한다. 추천 카드 제목은 두 줄 공간을 확보하되 분류와의 간격을 줄인다. 테마 배경과 구성은 유지하고 제목·설명은 사용자 확정에 따라 흰색으로 표시한다.
- 도장 화면의 미방문 장소도 이름과 탐험 핀을 표시하고 `방문 전`/`방문 완료`를 구분한다. 빈 상태에서 `스핀으로 장소 찾기`를 제공한다. 지역 선택은 밑줄 탭이다.
- 홈·설정·도장 목록은 탭 재진입마다 순차 등장시키지 않는다. 도장 헤더의 반복 부유는 제거하고 실제 도장 획득 팝은 유지한다. 지역 팝오버는 120ms 페이드만 적용하며 모션 감소 시 생략한다.
- 명소 전체 개수는 `둘러볼 곳 N곳`, 종류 필터는 `카페 N곳`처럼 표시한다. 설정의 문의 및 도움말은 기존 support.html로 연결한다.
- 설정의 `개인정보처리방침`은 새 탭으로 나가지 않고 앱 내부 하단 시트로 연다. 본문 원본은 `web/src/content/privacyPolicy.ts` 하나이며, 스토어에 등록된 공개 URL(`/privacy`)의 실체인 `web/public/privacy.html`과 어긋나면 `npm run check`가 실패한다.

## 사진 확대 뷰어 (2026-09-07)

스핀 종류 메뉴 후속 조정: 큰 세그먼트 배경을 제거하고 지도 핀·식기·커피 선형 아이콘과 텍스트를 나란히 배치한다. 선택 항목만 흰색 캡슐·옅은 파란 테두리로 강조하며, 46px 높이와 aria-pressed를 유지한다.

- 밝은 흰색 카드형 native dialog로 제목·닫기를 상단, 이전/다음·사진 수·출처를 사진 아래에 배치한다. 사진은 원본 비율을 유지하며 화면 높이에 맞춰 제한하고 어두운 고정 여백을 제거한다. Escape·닫기·바깥 클릭·하드웨어 뒤로가기를 지원한다.
- 좌우 드래그/터치를 따라 사진이 이동하고 충분히 밀거나 빠르게 밀면 240ms 슬라이드로 다음 사진에 정착한다. 짧은 드래그는 제자리로 복귀한다. 좌우 버튼·키보드도 같은 전환을 사용하고 첫/마지막 사진을 순환한다. 인접 두 사진만 미리 렌더링하며 모션 감소 시 즉시 전환한다. 세로 스크롤과 핀치 확대를 유지한다.
- 사진 없음·개별 이미지 실패·재시도를 표시한다. 확대창은 뷰포트 안에 맞추고 포커스를 제한한다. 기존 갤러리 API와 메모리 캐시 정책은 유지한다.
