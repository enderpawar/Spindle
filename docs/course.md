# 방향 기반 여행 코스 명세 v0.3

> 상태: **Phase 7 구현 기준**. Phase 1~6의 단일 추천 MVP를 대체하지 않고 결과 카드에서 선택적으로 확장한다.
> 추천 점수와 존 이동비용은 각각 `.claude/skills/algorithm/SKILL.md`, `docs/zones.md`를 그대로 사용한다.

## 1. 목적과 진입점

- 사용자가 스핀으로 정한 방향을 유지하면서 실제로 이어서 방문할 수 있는 2~4개 장소를 제안한다.
- 진입점은 S4 단일 추천 결과의 **[이 방향으로 코스 짜기]**다. 코스 기능 때문에 기존 "다른 후보 보기"·"다시 돌리기" 흐름을 변경하지 않는다.
- 최초 추천 장소가 코스의 첫 장소이자 방향 앵커다. 코스를 만들 때 스핀 결과나 첫 장소를 다시 추첨하지 않는다.

## 2. 입력과 출력

### 입력

- 단말 내 출발점 좌표
- 확정 방위각과 8방위 부채꼴
- 이동시간 다이얼(20분~하루 눈금의 분 예산, 하루 = 무제한)
- 현재 단일 추천 POI
- 실시간 호출 후 세션 메모리에 있는 POI 후보와 상세정보
- `docs/zones.md` 존-교량 데이터와 `docs/curation.md` 티어 데이터

### 출력

| 이동시간 예산 | 목표 장소 수 | 총 보정 이동시간 상한 |
|---|---:|---:|
| 20분 이하 | 2곳 | 예산(분) |
| 1시간 이하 | 3곳 | 예산(분) |
| 1시간 초과·하루 | 4곳 | 예산(분), 하루는 권역 내 무제한 |

- 총 보정 이동시간은 `출발점 → 첫 장소 → ... → 마지막 장소`의 구간별 이동비용 합계다.
- 각 장소에는 방문 순서, 이전 지점으로부터의 보정 이동시간, 운영상태, 장소별 길찾기 딥링크를 표시한다. 코스 전체는 1→2→3→4 순서를 지도에 표시한다.
- 시간은 존-교량 모델의 근사치이므로 정밀 경로 또는 도착시간으로 표현하지 않는다.

## 3. 후보 자격

후속 장소는 아래 조건을 모두 만족해야 한다.

1. 현재 코스에 같은 contentId가 없어야 한다.
2. 추천 점수 산식에서 운영 상태가 0이 아니어야 한다.
3. 원래 출발점에서 본 방위가 현재 코스의 기준 부채꼴에 속해야 한다.
4. 추가 후 총 보정 이동시간이 현재 다이얼의 분 예산을 넘지 않아야 한다 (하루는 무제한).
5. 세션 메모리에 존재하는 실시간 TourAPI 응답에서 얻은 후보여야 한다.

- 최초 추천이 인접 방위 확장으로 선정됐다면 해당 확장 부채꼴을 코스 기준 방위로 사용하고 기존 확장 사유를 유지한다.
- 후속 후보가 부족하면 확정 방위의 양쪽 인접 부채꼴까지 허용한다. 그래도 부족하면 장소 수를 줄이며, 코스만을 위해 전방위로 확장하지 않는다.

## 4. 구성과 방문 순서

1. 최초 추천 장소를 첫 장소로 고정한다.
2. 현재 위치(처음에는 첫 장소)에서 이동비용이 가장 낮은 적격 후보를 다음 장소로 선택한다.
3. 이동비용이 같으면 추천 점수가 높은 후보, 그마저 같으면 contentId 오름차순으로 선택해 결과를 재현 가능하게 한다.
4. 목표 장소 수에 도달하거나 더 이상 적격 후보가 없을 때 종료한다.

- 2곳 이상이면 목표 수보다 적어도 축소 코스를 제공하고 이유를 표시한다.
- 2곳 미만이면 코스를 생성하지 않고 S4 단일 추천 결과를 유지한다.
- 장소 상세 호출 하나가 실패하면 해당 후보를 제외하고 같은 규칙으로 한 번 재구성한다. 재구성 후 2곳 미만이면 S4로 폴백한다.

## 5. 개인정보·데이터 규칙

- 방향 판정, 후보 필터, 존 판정, 구간 이동비용, 방문 순서는 모두 단말 내에서 계산한다.
- 사용자 GPS 좌표·선택 출발점 좌표·방위각을 프록시나 외부 경로 API로 전송하지 않는다.
- POI 조회는 지역코드 기반 실시간 호출만 사용한다.
- POI 후보·상세 응답·생성 코스는 메모리/세션 범위에서만 유지하고 localStorage·IndexedDB·Cache Storage·파일·서버 DB에 저장하지 않는다.
- 지도 딥링크는 사용자가 장소별 [길찾기] 또는 코스 단위 [카카오맵에서 코스 길찾기]를 누를 때만 연다. 앱 내부에서 다중 경유 경로 API를 호출하지 않는다.

## 6. 필수 테스트

- 첫 장소와 기준 방위가 코스 생성 전후 동일하다.
- 분 예산 경계값(예: 20분·40분 정각)에서 포함·제외가 정확하다.
- 같은 contentId가 중복되지 않는다.
- 같은 입력 후보 집합이면 방문 순서가 동일하다.
- 목표 수 미달 시 2곳 이상은 축소 코스, 2곳 미만은 단일 추천 폴백으로 동작한다.
- 바다 방향에서 인접 방위 확장 사유가 코스 화면에도 유지된다.
- 네트워크 요청에 사용자 좌표·방위각이 없고 영속 저장소에 API 응답·코스가 남지 않는다.

## 7. 카카오맵 코스 길찾기 위임

> 상태: **구현 완료(2026-08-02, 단계식으로 개편)** — `web/src/lib/mapLinks.ts`의 `kakaoMapCourseWalkUrl()` + `CourseScreen`의 2단계 안내.
> **단말 내 탐험형 안내(자체 경로안내)는 폐기했다.** 길찾기는 전부 카카오맵에 위임하고, Spindle은 "지금 어느 단계인지"만 관리한다.

- S6 미리보기의 카카오맵 위에는 현재처럼 확정 코스의 1→2→3→4 순번 핀과 구간선을 표시한다.
- 코스 안내는 화면 이동 없이 **S6 안에서 2단계로** 진행한다. 하단 기본 CTA `코스 안내 시작`을 누르면 단계 카드가 열린다.
  - **1단계 — 1번 장소로 이동**: `카카오맵으로 길찾기`가 목적지 전용 `https://map.kakao.com/link/to/{1번 장소명},{위도},{경도}`를 연다. 그 아래 `도착했어요 · 다음 단계로` 버튼이 2단계로 넘긴다.
  - **2단계 — 코스 순서대로 이동**: `카카오맵에서 전체 코스 보기`가 `https://map.kakao.com/link/by/walk/{장소명},{위도},{경도}/...`를 열어 1→2→3→4 방문 순서를 보여준다. `1단계로 돌아가기`로 되돌릴 수 있다.
  - 단계 전환은 **오직 사용자 확인 동작**으로만 일어난다. Spindle은 도착을 판정하지 않으므로 GPS·나침반을 구독하지 않는다. 두 링크 모두 사용자가 누를 때만 새 컨텍스트로 연다.
  - 안내 진행 중 하단 CTA는 `안내 종료`로 바뀌어 언제든 미리보기 상태로 돌아갈 수 있다.
- 1단계에서 사용자가 도착을 확인하면 1번 장소를 방문 처리(도장)한다. 길찾기 링크를 누른 시점에도 동일하게 방문 처리하며, 두 경로 모두 좌표를 읽지 않고 사용자의 명시적 행동만 근거로 삼는다.
- URL에는 TourAPI로 받은 **공개 관광지 이름·좌표와 확정 방문 순서만** 포함한다. 사용자 GPS, 기기 방위각, GPS 정확도, demo 가상 위치, 선택 출발점 좌표는 포함하지 않는다. 현재 위치는 목적지 전용 링크가 열린 뒤 카카오맵이 자체 권한과 정책으로 정하며 Spindle은 이를 읽거나 전달하지 않는다.
- Spindle은 경로를 조회하거나 도로 회전 정보를 해석하지 않는다. 실제 도보 경로 계산, 현재 위치 사용 동의, 재탐색, 도착 판정, 턴바이턴 안내는 전부 이동한 카카오맵 화면이 담당한다.
- 이 방식은 Kakao Map REST API, Kakao Navi SDK, Spindle 프록시를 호출하지 않는 URL 연결이므로 별도 REST 토큰이 필요하지 않다. 카카오맵 앱 강제 실행이나 앱 내부 턴바이턴을 보장한다고 표현하지 않는다.
- 모바일에서는 카카오맵이 제공하는 모바일웹/앱 연결 정책을 따르고, PC에서는 카카오맵 웹 길찾기를 연다. 팝업 차단 또는 외부 앱 미설치 시 현재 화면의 장소별 링크를 계속 사용할 수 있어야 한다.
- 장소명은 URL 인코딩하고 유효한 위·경도가 없는 POI(비유한값·(0,0)·범위 밖)는 코스 링크에서 제외한다. 제외 후 2곳 미만이면 빌더가 `null`을 돌려주고 화면은 코스 CTA를 숨긴 채 장소별 [길찾기]만 제공한다.
- 자동 테스트(`web/src/lib/mapLinks.test.ts`)는 1번 장소 목적지 전용 링크, 첫 장소 좌표 누락 시 2번으로 건너뛰지 않는 폴백, `walk` 이동수단, POI 순서, 한글·특수문자 인코딩, 2~4곳 경계, 그리고 URL에 사용자 위치·방위 데이터가 들어가지 않음을 고정한다.

## 8. 스핀 코스 모드

- 스핀 화면의 `코스` 모드는 기존 결과 화면 진입과 별개인 빠른 진입점이다. 한 번의 스핀 방위로 단일 추천과 코스를 연속 계산하며, 첫 추천 장소와 확정 방위각을 코스 생성 중 바꾸지 않는다.
- 생성 성공 시 S6 미리보기를 표시한다. 실패하면 첫 추천 결과를 유지하고 `이 방향은 코스로 잇기 어려워요`와 `코스 다시 돌리기`를 제공한다.
- 코스 모드와 테마 여정은 독립 기능이며 동시에 적용하지 않는다. 진행 중 테마에서 코스 모드를 고르면 명시적 안내 문구와 함께 테마 여정을 종료한다.

### 자체 경로안내를 두지 않는 이유 (2026-08-02 결정)

- 단말 내 방위 화살표·거리·도착 판정으로 구성한 **탐험형 안내는 폐기**했다. 관련 화면(`CourseGuideScreen`), 도착 판정 엔진(`engine/courseGuidance`), demo GPS 조이스틱을 모두 제거했다.
- 실제 도로 경로를 안내하려면 라우팅 API에 **사용자 현재 좌표를 출발지로 전송**해야 한다. 이는 절대 원칙 1을 위반하고, 공모전 FAQ 기준("서버로 전송하면 DB 저장 여부와 무관하게 신고 대상")에 따라 위치기반서비스사업자 신고 대상으로 전환된다. Kakao Maps JS SDK에는 경로 탐색 기능이 없고, 카카오모빌리티 길찾기는 REST 키가 필요한 별도 서버 API다.
- 방위 화살표만 제공하는 절충안은 도로를 해석하지 못해 카카오맵보다 열등하면서도 GPS 상시 구독을 요구한다. 심사위원은 서울에서 URL만 열어 시연하므로 체감 이득도 없다.
- 결과적으로 앱은 **GPS·나침반을 코스 흐름에서 전혀 읽지 않는다.** 좌표 무전송이 구조적으로 자명해지고, 길찾기 품질은 카카오맵 수준을 그대로 얻는다.
