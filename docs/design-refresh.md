# 디자인 수정 계획 — 2026-09-07

PLAN.md Phase 5 폴리시 / Phase 7 도장·명소 화면의 후속 수정. 사용자 요청에 따라 계획 수립 후 localhost 검토본을 구현한다. 스토어 배포는 최종 디자인 확정 이후 진행한다.

## 목표와 순서

1. [x] 시간 선택: 실제 엔진 입력인 이동시간 한도를 명시하고 현재 선택값을 가장 강조한다. 분위기 라벨(반나절)과 무제한 값의 하루 표기를 슬라이더에서 제거한다. 계산·추천 로직은 그대로 유지한다.
2. [x] 도장: 미방문 장소 이름을 공개하고 탐험 표식으로 표현한다. 첫 방문 안내에 스핀 진입 버튼을 제공하고 방문/미방문 상태를 텍스트로 구분한다. 지역 탭은 명소와 같은 밑줄 표현을 적용한다.
3. [x] 홈: 오늘의 미션을 히어로 아래의 짧은 안내로 이동한다. 도장 진행을 작은 진행 막대로 압축하고 추천 카드 간격과 테마 글자 대비를 개선한다. 브랜드·히어로 이미지·기존 테마 구성은 유지한다.
4. [x] 화면 정리: 스핀 목적 설명과 조작 안내를 분리한다. 명소 데이터 수를 사용자 언어로 요약하고 설정에 기존 지원 페이지를 연결한다.
5. [x] 모션: 기존 원판 감속·결과·도장 획득 연출은 유지한다. 홈/설정/도장 재진입 시 반복 순차 등장과 도장 헤더의 무한 부유를 줄인다. 지역 메뉴에는 짧은 페이드만 적용하고 모션 감소 설정에서는 제거한다.
6. [x] 검증: check/build, 280/390/480px 브라우저 레이아웃, 시간 키보드 조절, 도장 초기/획득 상태, 지역 선택, 모션 감소 확인. 미리보기 스크린샷을 남긴다.

## 완료 범위와 후속 확인

### 하단 내비게이션 추가 검토

- 2026-09-07: 스핀 48px·약 8px 돌출, 현재 탭 캡슐, 5개 라벨 기준선 통일, 그림자 약화 적용. 390px 홈·스핀 스크린샷 비교에서 콘텐츠 간섭 감소와 선택 구분을 확인해 검토본을 유지한다. 사용자 선호에 따라 이 변경만 복원 가능하다.
- 직전 파일 보관: `C:/Users/user/AppData/Local/Temp/spindle-nav-before/BottomNav.tsx`, `C:/Users/user/AppData/Local/Temp/spindle-nav-before/index.css`. 이후 CSS 수정이 추가되면 전체 CSS를 덮어쓰지 말고 내비게이션 변경만 되돌린다.

검증 완료(2026-09-07): `npm run check`(web 359개·proxy 26개) 및 `npm run build` 통과. Edge 자동 브라우저에서 5개 탭×280/390/480px 가로 넘침 없음, 시간 키보드 최소·1분 증가·무제한 값, 도장 방문 전/완료, 지역 선택·카페 혼잡 숨김, 모션 감소 설정, 런타임 오류 0건을 확인했다. 390px 홈·도장·스핀 스크린샷 육안 검토 완료. 기존 Fast Refresh lint 경고는 남아 있다.

이번 범위는 위 항목의 localhost 검토본이다. 지도 핀 라벨 정책·추천 엔진·센서·API·수집 방식은 별도 기능 변경이므로 포함하지 않는다. iOS/Android 실기기에서 안전영역과 네이티브 스핀 조작은 배포 전 확인한다. 설정 버전 표시는 빌드 버전과 연결하는 출시 작업에서 다룬다.
