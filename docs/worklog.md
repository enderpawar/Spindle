# 작업 일지

> 세션 단위 기록. **다음 세션은 이 문서의 맨 위(가장 최근 항목)부터 읽는다.**
> 규약·명세는 여기 적지 않는다 — 그건 `AGENTS.md`·`SPEC.md`·`PLAN.md`의 몫이고,
> 여기에는 **"지금 어디까지 왔고 무엇이 대기 중인가"** 와 실행하며 알게 된 함정만 남긴다.

---

## 2026-09-11 — 좁은 폰에서 390px 배치 유지 (뷰포트 고정 폭)

- 증상: iPhone `디스플레이 확대/축소 → 더 큰 텍스트`에서 명소 화면의 부제목이 두 줄로 갈라지고, 종류 탭 옆 `모든 지역`이 다음 줄로 떨어졌다. 홈도 비슷하게 흐트러진다는 제보가 있었다.
- 원인: 확대 모드의 CSS 뷰포트는 **320px**이다. 스크린샷 수치가 320px 레이아웃과 정확히 맞았다.
  - `index.css`의 `@media (max-width: 320px)`이 부제목을 `max-width: 116px`에 가둔다.
  - 필터 바는 종류 스위치의 flex-basis 230px과 지역 버튼 약 94px이 합쳐 약 332px라, **360px(갤럭시)에서도 이미 줄바꿈되고 있었다.**
- 해결: 요소별 땜질 대신 `web/src/native/viewport.ts`를 넣었다. 화면 짧은 변이 390 미만이면 viewport meta를 `width=390, viewport-fit=cover`로 바꿔 모든 화면을 390 배치 그대로 축소한다. 390 이상 기기는 변화가 없다. `main.tsx`가 첫 렌더 전에 호출한다.
- **함정 — Android WebView는 기본값에서 meta의 `width=`를 무시한다.** `MainActivity`에서 `setUseWideViewPort(true)`와 `setLoadWithOverviewMode(true)`를 켰다. 이 변경은 새 AAB로만 반영된다.
- **함정 — 데스크톱 Chrome은 viewport meta를 무시한다.** 창 크기만 줄여서는 효과가 안 보인다. DevTools 기기 툴바에서 Mobile 타입 기기(320×693 사용자 지정, 360, 375)로 확인해야 한다.
- 감수한 점: 확대 모드 사용자에게 앱 안 글자가 기본 모드와 같은 물리 크기로 보인다. 요구가 배치 유지였다.
- PR #20 리뷰(서브 에이전트 medium) 지적 두 건을 반영했다.
  - **함정 — 가로에서도 고정하면 프레임이 짓눌린다.** Android 앱(`screenOrientation` 없음)과 모바일 브라우저 탭은 가로로 돈다. 360×740을 가로로 들면 390 폭이 약 1.9배 확대되어 앱 높이가 190px 남짓이 된다. `(orientation: portrait)`일 때만 고정한다.
  - **함정 — `getBridge()`는 null일 수 있다.** 시스템 WebView가 없으면 `BridgeActivity`가 `no_webview` 화면만 띄우고 브리지를 만들지 않는다. null 확인 없이 설정을 건드리면 그 안내 화면이 크래시로 바뀐다.
- 검증: `npm run check` 통과(web 431 + proxy 26), `npm run build` 통과, Android `:app:compileDebugJavaWithJavac` 통과.

### 다음 대기

- 사용자 Chrome 기기 모드 확인.
- **iPhone 확대 모드 실기기 확인이 필요하다.**
  - 축소 배율에서 `mobile-pwa.css`의 safe-area 보정(상단 노치·하단 홈 인디케이터)이 맞는지 본다.
  - 지도 핀 탭 위치와 입력창 포커스 확대도 본다.
  - 문제가 있으면 `main.tsx`의 호출 한 줄로 되돌린다.
- iOS·Android 앱 반영에는 새 빌드가 필요하다(웹 번들 내장).

---

## 2026-09-09 — 1.1.0 배포·TestFlight·스토어 자산 (웹 배포 완료, iOS 심사 제출)

- PR #19 머지(`ab5235b`) 후 Cloudflare 프로덕션 배포를 확인했다. 배포된 CSS에 리뷰 수정본(`.result-visit-skeleton`의 `margin-top:34px`·`min-height:50px`)이 들어 있는 것으로 검증했다 — **번들 해시 비교는 쓸 수 없다.** CI가 `VITE_KAKAO_JS_KEY`를 주입해 로컬 빌드와 해시가 항상 다르다.
- TestFlight v1.1.0 **빌드 19** 업로드 성공(run `34324537761`). 인증서 한도에 걸리지 않았다 — 9/9 새벽 정리 이후로는 재발이 없다. 사용자가 실기기 점검을 마쳤다.
- Android **vc6 / 1.1.0** AAB를 이 머신에서 빌드했다(8.62 MB, `jar verified`, 지문이 vc2~vc5와 동일). **Play Console 업로드·`검토를 위해 변경사항 제출`은 사람이 한다** — 이 콘솔에 API 액세스가 없고 fastlane에도 Android 레인이 없다.
- **함정 1 — `export:ios`가 900초 타임아웃으로 죽고 있었다.** 진짜 원인은 `tools/store-screenshots/tailwind.config.ts`가 ESM으로 로드되는데 `plugins: [require("tailwindcss-animate")]`를 쓴 것. Node 24부터 `ReferenceError: require is not defined`가 나고 **루트 페이지 CSS 컴파일이 통째로 실패**한다 → 에디터가 프로젝트 파일을 못 읽고 기본값(locale `en`, 없는 샘플 이미지)으로 떨어짐 → 20개 렌더 전부 실패 → 다운로드 이벤트가 안 옴. 헤드리스라 그 토스트가 안 보여 증상은 타임아웃으로만 나타났다. **다음에 export가 조용히 타임아웃하면 `next dev` 로그부터 본다.**
- **함정 2 — 스크린샷 캡션이 `원도심 55곳`이었는데 실제는 48곳이다** (영도구 14 + 동구 11 + 서구 10 + 중구 13). `mock/stamps.ts`의 `zones`가 `POI_POOL`을 이 4개 구로 걸러 만들고, 앱 홈에도 `0/48`로 뜬다. iOS·Android 덱 양쪽을 고쳤다.
- 스크린샷을 프로덕션 기준으로 다시 구웠다. 하단 내비 라벨이 목업 프레임에 잘리던 기존 이슈도 해소됐다 — `capture-web`이 홈 인디케이터 여백 34px를 주입하기 때문이다.
- 홈 캡처에서 `추천 여행지`는 제목만 보이고 카드가 접힌다. 테마 그리드가 위로 들어와 한 화면에 히어로·테마·추천을 다 넣을 수 없고, 스크롤을 주면 히어로가 통째로 날아간다 — 히어로+빠른진입+테마를 보여주는 쪽을 골랐다.

### 다음 대기

- **Android**: `web/android/app/build/outputs/bundle/release/app-release.aab`를 Play Console 프로덕션에 올리고 `게시 개요 → 검토를 위해 변경사항 제출`. 릴리스 노트는 `fastlane/metadata/android/ko-KR/changelogs/6.txt`.
- **iOS**: **심사 제출 완료** (run `34357709903`, 빌드 19). 스크린샷 5장 교체·precheck 무경고 통과·승인 시 자동 공개. 결과만 확인하면 된다.

---

## 2026-09-09 — Phase 5 추천 여행지 패널 제거 확정

- 사용자 확정 방향으로 추천 영역의 반투명 흰 패널·테두리·그림자·내부 패딩을 제거했다. 테마와 32px 간격, 좌우 20px(360px 미만 16px) 정렬선으로 묶고 사진·본문을 배경 위에 직접 배치한다. 390px 기준 대표 사진의 CSS 가용 폭은 316px에서 350px로 넓어진다.
- 사진 모서리·목록 구분선·44px 전체 명소 링크·출처·기존 동선을 유지한다. 배경은 변경하지 않았다. ui.md·SPEC.md의 패널 설명을 최종 설계로 교체했다.
- `npm.cmd run check` 통과(web 415 + proxy 26, 기존 Fast Refresh 경고), `npm.cmd run build` 통과, `git diff --check` 통과. localhost:5176 HTTP 200 확인.
- 현재 연결된 브라우저가 없어 이번 변경의 렌더링·iOS 실기기 육안 확인은 대기다. 이전 패널 버전의 캡처 검증을 이번 결과로 재사용하지 않았다. 웹·스토어 배포는 하지 않았다.

---

## 2026-09-09 — Phase 5 홈 첫인상 마감

- 사용자 재량 요청에 따라 기존 바다빛 배경에 정적인 얇은 항해 곡선 두 겹을 더했다. 추가 이미지·반복 애니메이션 없이 CSS로 구성했다.
- 앞선 대기 작업인 추천 여행지 반투명 흰 패널을 완료했다. 제목·링크·사진·목록·출처를 한 표면으로 묶고 대표 카드의 흰 바탕·그림자를 걷었다. 좁은 폭에서는 썸네일을 64px로 줄인다. 실제 홈 순서(테마 다음 추천)에 맞춰 ui.md의 오래된 순서도 바로잡았다.
- 검증: `npm.cmd run check` 통과(web 415 + proxy 26, 기존 Fast Refresh 경고), `npm.cmd run build` 통과. 임시 Playwright 스크립트로 280/320/390/480px 가로 넘침 없음과 스핀 버튼 진입 확인, 캡처를 직접 열어 첫 화면·추천 패널 검토. 캡처는 OS 임시 폴더 `spindle-home-polish`에 있고 스크립트는 검증 후 제거했다.
- localhost:5176에서 검토 가능. 새 디자인의 iOS Safari 실기기 확인·스토어 스크린샷 갱신·배포는 대기. 이번 작업은 로컬 수정과 커밋 범위다.

---

## 2026-09-09 — 홈 배경 앰비언트·섹션 제목 정리

- 홈 프레임 배경을 평평한 `--l-bg` 대신 층을 쌓은 앰비언트로 바꿨다(`.home-screen`). 세로 그라데이션 + 오른쪽 위 바다빛·왼쪽 라벤더·아래 모래빛 워시 + 26px 점 결. 스크롤 컨테이너가 아니라 프레임에 칠해 콘텐츠만 흐르고 배경은 제자리에 남는다. 이걸 붙이려고 `ScreenFrame`에 `className` 옵션을 추가했다(선택 prop이라 다른 화면은 그대로).
- 사용자 요청으로 홈의 섹션 제목 텍스트를 뺐다. `어떤 부산을 만나볼까요?`는 테마 그리드의 `role="group" aria-label`로, `발견해볼 부산`은 섹션 `aria-label`로 남겼다(ResultScreen에서 쓰던 방식과 같다). 안내 문구 `익숙한 여행지 너머, 이런 곳도 있어요`도 뺐다.
- 목록만 남으니 휑하다는 판단으로 고정 큐레이션 영역에만 제목 `추천 여행지`와 `전체 명소 ›`를 되살렸다. 링크는 제목 아랫줄 오른쪽 끝.
- **함정 1**: `전체 명소` 버튼의 `min-height: 44px`가 그대로 레이아웃 높이가 되어 12px 글자 위아래로 16px씩 빈 공간이 생겼다. 제목 블록이 78px까지 부풀었다 — 음수 마진(`margin: -11px 0 -8px`)으로 히트 박스는 44px로 두고 보이는 여백만 걷어냈다.
- **함정 2**: 링크 우측 정렬을 `display: block; margin-left: auto`로 했는데, 이건 버튼이 `display: block`에서도 shrink-to-fit으로 남는 동작에 기대는 방식이다. flex column + `align-self: flex-end`로 바꿔 확정적으로 만들었다.
- 대비는 계산해서 맞췄다. 배경 위에 직접 놓이는 보조 문구(#526a8d)가 배경이 가장 어두워지는 지점에서 4.86:1 (기존 평면 배경은 5.18:1). AA는 넘지만 이 배경 위에 더 옅은 회색을 쓰면 깨진다.
- 검증: `npm run check` 통과(web 415 + proxy 26, 기존 Fast Refresh lint 경고만). **실기기·브라우저 육안 확인은 대기** — 규약대로 브라우저 자동화는 쓰지 않고 dev 서버 URL만 사용자에게 넘겼다.
- 이 커밋 직전에, 앞선 세션들이 커밋하지 않고 남긴 변경(개인정보 시트·결과 카드 순서·공유 카드·테마 아이콘·스토어 스크린샷)을 `b2a117b`로 먼저 묶어 커밋했다.

### 다음 대기

- 사용자가 고른 다음 단계가 **미착수**다: `추천 여행지` 블록 전체를 반투명 흰 패널로 감싸 제목이 표면에 속하게 만들기(제목이 배경 위에 혼자 떠 보인다는 피드백). 패널 안에서 대표 카드가 흰색 위 흰색이 되지 않게 카드 크롬을 걷어내는 작업이 따라온다.
- 스토어 스크린샷 `3_home.png`(android/ios/web)은 이번 홈 변경 이전 화면이라 다시 찍어야 한다.

---

## 2026-09-09 — 결과 카드 정보 순서·공유 카드 방위 배지 정리

- 결과 카드 본문을 `주소 → 방문 정보 → 이곳의 매력 → 지도` 순으로 바꿨다. 사용자와 순서를 논의한 결과다: 길찾기 CTA가 하단 액션 바에 고정돼 있어 카드 안 지도는 확인용이고, 방문 정보와 소개문은 "갈 수 있나 → 가고 싶나"로 이어지는 한 덩어리라 그 사이에 제일 무거운 지도를 끼우지 않는다.
- `방문 정보`·`이곳의 매력` 제목 행을 없앴다(사용자 요청). 화면에서 뺀 제목은 `aria-label`로 남겨 스크린리더에는 그대로 읽힌다. 방문 정보 스켈레톤의 제목 자리표시자와 쓰이지 않게 된 h3 CSS도 함께 제거했다.
- 공유 카드에서 `○쪽으로 만난 부산` 둥근 방위 배지를 제거했다. `ShareCardInput.directionLabel`과 폰트 워밍 문자열에서도 뺐다. **배지를 지우기만 하면 메시지와 구분선 사이가 290px가량 비어 카드가 아래로 휑해진다** — 사진 하단을 1264→1400으로 늘리고 메시지 시작을 1504로 내려 배지가 있던 때와 같은 여백 리듬을 유지했다.
- 검증: `npm run check` 통과(web 415 + proxy 26), `node tools/store-screenshots/verify-share-card.mjs http://localhost:<port>` PASS. 생성된 PNG를 실제로 열어 배지 제거와 공백 보정을 육안 확인했다(임시 폴더 `spindle-share-review`).
- **함정**: 이 환경의 Vite dev 서버는 `localhost`(IPv6)에만 바인딩돼 `127.0.0.1`로는 붙지 않는다. verify 스크립트 기본값이 `http://127.0.0.1:5174`라 그대로 돌리면 `Failed to fetch .../@react-refresh`로 죽는다 — base 인자에 `http://localhost:<port>`를 넘겨야 한다.
- 결과 화면의 실기기 확인은 대기. 특히 방문 정보가 맨 위로 오면서 `detailIntro2` 로딩 중 카드 상단이 스켈레톤으로 시작하는데, 첫인상이 괜찮은지는 실기기에서 봐야 한다.

---

## 2026-09-09 — 설정 › 개인정보처리방침을 앱 내부 시트로 전환

- 설정의 `개인정보처리방침`이 `<a href="/privacy.html" target="_blank">`라 새 탭/외부 브라우저로 이탈했다. 사용자 요청대로 앱을 벗어나지 않는 하단 시트(`components/PrivacySheet.tsx`)로 바꿨다. 마크업·모션은 ResultScreen의 [자세히 보기] 시트를, 뒤로가기는 `useBackGuard`를 그대로 재사용했다.
- 방침 본문의 단일 원본을 `src/content/privacyPolicy.ts`로 두고, 스토어 공개 URL의 실체인 `public/privacy.html`은 **삭제하지 않고 유지**했다 (Play/App Store에 등록된 `/privacy`). 두 본문이 갈라지면 `content/privacyPolicy.test.ts`가 `npm run check`에서 실패한다 — 실제로 한 문장을 고쳐 실패하는 것을 확인하고 되돌렸다.
- **함정**: 오버레이 z-index를 ResultScreen 시트(13) 기준으로 잡았더니 `BottomNav`(zIndex 20) 뒤로 들어가 마지막 절(8. 문의처)이 하단 내비에 가렸다. ResultScreen에는 BottomNav가 없어서 드러나지 않던 차이다 — 탭 화면 위 오버레이는 20보다 위여야 한다(현재 30).
- 방침 시트에는 `출처: ⓒ한국관광공사`를 넣지 않기로 했다(사용자 판단). 이 시트는 공공데이터를 그리는 화면이 아니라 방침 문서이고, 출처 표기는 실제 TourAPI 데이터를 그리는 화면(`SourceLine`)과 스토어 제출용 `public/privacy.html`이 이미 지고 있다. 후자의 출처 유지는 테스트로 고정했다.
- 검증: `npm run check` 통과(web 415 + proxy 26, 기존 Fast Refresh lint 경고만), `npm run build` 통과하고 `dist/privacy.html`이 그대로 생성됨을 확인했다. **실기기 확인은 대기** — 안드로이드 하드웨어 뒤로가기로 시트만 닫히는지, iOS Safari에서 긴 본문 스크롤과 safe-area 여백이 맞는지는 아직 보지 않았다.
- 브라우저 자동화는 규약대로 쓰지 않았다. dev 서버(`npm run dev`)만 띄우고 육안 확인은 사용자에게 넘겼다. 커밋·배포는 하지 않았다.
- 다른 에이전트의 미커밋 변경(HomeScreen·ThemeNavigation·tools/store-screenshots)은 건드리지 않았다.

---

## 2026-09-09 — Phase 5 홈 발견 영역·정보 위계 개선

- 사용자 확정안(홈 전체 정돈, 대표 1곳+압축 3행)을 구현했다. `발견해볼 부산`을 테마 위에 배치하고 깡깡이 예술마을의 기존 큐레이션 이야기를 보여준다. 고정 목록을 일일 추천으로 표현하지 않으며 출처를 명시했다.
- 브랜드·해변·별이와 기존 이동 콜백을 유지하고, 홈 CSS Module로 카드·바로가기·테마를 정리했다. 다른 작업의 공용 CSS·PhotoViewer·프록시·SpotsCategoryNavigation 변경은 수정하지 않았다.
- 검증: `npm run check` 통과(web 401 + proxy 26, 기존 Fast Refresh lint 경고), `npm run build` 통과. 임시 Playwright 스크립트로 280/320/390/480px 가로 넘침 없음, 44px 터치 영역, 첫 화면 추천 제목, 미션/하단 메뉴 간섭 없음, 스핀·장소 상세·전체 명소·테마·미션 이동을 확인했다. 실제 네 사진 로드, API 실패 폴백, 방문 0/1 상태, 키보드 포커스/Enter 이동도 확인했다. 스크립트 초기 실패는 링크를 버튼으로 찾은 선택자와 `/src/api`까지 가로챈 테스트 라우트 오류였고 수정 후 통과했다.
- 화면 캡처 및 검증 스크립트는 OS 임시 폴더의 `spindle-home-review`, `spindle-home-check.cjs`, `spindle-home-fallback.cjs`에 있다. 이미지 로드 후 대표 카드·목록과 280/390px 상하단을 육안 검토했다. Safari/iOS 실기기 검증과 이탈률 효과 검증은 수행하지 않았다.
- 기존 Vite localhost:5173·프록시 8787을 재사용했다. 브라우저 자동 열기는 실행 정책으로 거부됐으므로 링크를 전달한다. 웹·스토어 배포는 하지 않았다.

---

## 2026-09-09 — Phase 5 공유 카드 디자인 개선

- 사용자 요청에 따라 Astra가 공유 PNG를 밝은 블루·화이트 여행 엽서로 디자인·구현했다. 옛 4각별 대신 현행 브랜드 마크를 사용하고 장소명·대표 사진을 크게 배치했다.
- 공유 화면은 생성된 PNG를 그대로 미리 보여주고 같은 Blob을 저장·공유한다. 생성 중/실패·재시도, 사진 없음·실패 시 바다/나침반 폴백, 긴 장소명 자동 조절을 제공한다. 생성 결과는 화면 메모리에만 유지한다.
- 검증: `npm run check` 통과(web 401 + proxy 26), `npm run build` 통과. `node tools/store-screenshots/verify-share-card.mjs`로 합성 데이터의 280×640·390×844·480×844·390×568 레이아웃, 긴 이름, 이미지 유무/실패, 실제 1080×1920 PNG 다운로드를 확인했다. 캡처는 OS 임시 폴더 `spindle-share-review`에 있다.
- 웹/스토어 배포는 하지 않았다. 새 카드의 iOS/Android 실제 공유 시트 전달 확인은 대기.

---

## 2026-09-08 — Android vc5 출시 제출 · 기능설명서 화면 캡처

### 지금 상태

| 대상 | 상태 |
|---|---|
| 웹 (`spindle-6vp.pages.dev`) | ✅ 최신 (`main` `994ad9f`). 심사 제출용 URL |
| iOS App Store | ⏳ 1.0.9 심사 중 (승인 시 자동 공개) |
| Android Play | ⏳ **vc5 / 1.0.9 검토 제출 완료.** 승인 시 자동 공개 (관리형 게시 꺼짐) |
| 기능설명서 | ✅ 부록 A 화면 캡처 11장 추가 |
| 제출 양식 입력값 | ✅ `docs/submission/제출-양식-입력값.md` 신설 |

### 한 일

- **Android 릴리스 환경을 이 머신에 갖췄다.** 키스토어를 이전 빌드 머신(집 PC)에서 받아
  `web/android/`에 배치했고 지문이 vc2~vc4와 일치함을 확인했다. JDK 21·Android SDK 36을
  `C:\Users\user\devtools\`에 설치했다. 업로드 키 재발급은 불필요해졌다.
- **vc5(1.0.9) AAB 빌드·서명 검증·프로덕션 100% 제출.** `web/` 소스가 iOS 1.0.9
  바이너리(`ee2031a`)와 변경 0건임을 확인하고 올렸다.
- **기능설명서에 부록 A(화면 캡처 11장)를 넣었다.** 프로덕션에서 촬영하며 문서가 현재 앱과
  어긋나는 곳(존재하지 않는 `돌리기` 버튼, README 3분 시연 경로의 출발점·코스 진입)을 함께 고쳤다.
- **제출 양식 입력값 문서를 만들었다.** 글자 수별 소개 문안과 심사위원 접속 안내를 포함한다.

### 알게 된 함정

1. **`local.properties`의 백슬래시** — 하나만 쓰면 Java Properties가 유니코드 이스케이프로
   읽어 `Malformed \uxxxx encoding`으로 빌드가 죽는다. 슬래시로 쓴다.
2. **Play는 업로드만으로 심사가 시작되지 않는다** — `게시 개요`에서
   `검토를 위해 변경사항 N개 제출`을 눌러야 `검토 중`으로 바뀐다.
3. **이 Play Console에는 `API 액세스` 메뉴가 없다** — 설정·개발자 계정 어디에도 없어
   서비스 계정 자동 업로드를 접고 수동 업로드했다.
4. **프로덕션 게시일이 문서와 달랐다** — 문서 2026-09-07, 콘솔 실제 2026-09-03.
   날짜가 필요한 판단은 콘솔을 본다.
5. **캡처 스크립트는 `capture-web.mjs`와 분리해야 한다** — 그쪽 파일명·1080x2400·상태바
   띠는 스토어 자산 파이프라인의 계약이다. 문서용은 `capture-doc.mjs`(`npm run capture:doc`).

### 대기 중

- iOS·Android 심사 승인 (양쪽 모두 자동 공개 설정)
- 기능설명서 남은 확인 항목: 데이터랩 최신 수치 보강, mermaid 도식 이미지화,
  **실기기 검증**, **TourAPI 운영계정 호출 이력 확인**
- 1차 서류 제출 (마감 2026-09-21 16:00, 최소 하루 전)

---

## 2026-09-07 ~ 09-08 — 트래픽 수정·웹 배포·App Store 1.0.9 제출

### 지금 상태

| 대상 | 상태 |
|---|---|
| 웹 (`spindle-6vp.pages.dev`) | ✅ 최신 (`main` `3c81015`). **심사 제출용 URL이다** |
| 프록시 (`spindle-proxy.enderpawar.workers.dev`) | ✅ 최신 |
| iOS TestFlight | ✅ 1.0.9 (빌드 17) — 실기기 검증 통과 |
| iOS App Store | ⏳ **1.0.9 심사 중.** 승인 시 자동 공개 설정 — 사람이 누를 것 없음 |
| Android Play | ⛔ vc4 / 1.0에 멈춤. 아래 "대기 중" 참조 |

### 대기 중 (다음 세션이 이어받을 것)

1. **Android vc5 출시** — 코드·버전·changelog는 준비 끝(`versionCode 5`/`versionName 1.0.9`,
   `changelogs/5.txt`). **막힌 이유는 환경이다**: 이 머신에 릴리스 키스토어도, JDK도,
   Android SDK도 없다. vc1~vc4는 다른 머신(집)에서 빌드했고 키스토어도 거기 있다.
   사용자가 집에서 빌드해 Play Console에 직접 올리기로 했다. 절차는 `GOOGLE_PLAY_RELEASE.md`
   2절 "다음 Android 릴리스에서 할 일".
2. **App Store 심사 결과 확인** — 승인되면 자동 공개된다. 리젝되면 사유를 보고 대응.
3. **`detailIntro2` 방문 정보 실기기 확인** — 09-07 일일 트래픽이 소진돼 그날은 검증하지
   못했다. 자정 리셋 후 결과 카드에 관람시간·휴무일이 뜨는지 본다.
4. **다른 화면의 `<img>` 롱프레스** — 스핀 버튼은 해결했지만 홈 히어로·별이 마스코트·명소
   카드 사진은 미확인. 브래킷이 나오면 같은 처리를 적용한다(아래 함정 2).

### 이번 세션에 한 것

- **TourAPI 호출 한도(429) 해소** — 세션당 `detailIntro2` 약 54회 → 5~7회.
  원인은 API가 아니라 **방향이 정해지기 전에 큐레이션 49곳을 전부 예열**하던 코드였다.
  예열을 스핀 후보 3곳·명소 목록 앞쪽 12곳으로 옮기고, in-flight 중복 방지와 429 지수
  백오프·엔드포인트별 쿨다운을 넣었다. (`bc67b10`)
- **웹 프로덕션 배포** — `origin/main`이 09-02에 멈춰 있어 **심사 제출용 URL이 3주 전
  코드**였다. iOS만 작업 브랜치에서 직접 dispatch해 최신이었다. 29커밋을 머지해 해소. (`cf49159`)
- **스핀 버튼 롱프레스 브래킷 제거** — 두 번 만에 잡았다(함정 2). (`0a0b960` → `ee2031a`)
- **스토어 스크린샷 재생성 경로 신설** — `capture:web`으로 라이브 웹앱에서 직접 뽑는다.
  에뮬레이터·실기기가 필요 없어졌다. (`971e9d0`)
- **`submit` 레인 신설** — 빌드 없이 심사 제출(함정 1). (`971e9d0`)
- **App Store 1.0.9 심사 제출** — precheck 무경고, 스크린샷 중복 정리, 자동 공개. (`3c81015`)

### 실행하며 알게 된 함정

**1. iOS 빌드는 몇 번에 한 번씩 반드시 인증서 한도로 죽는다.**
CI 러너는 매번 새 머신이라 키체인이 비어 있고, 클라우드 서명이 **빌드마다 새 Apple
Development 인증서를 발급한다.** 로그에는 `No profiles for 'kr.spindle.app'`이 먼저
보이지만 원인은 `maximum number of certificates`다 — 프로파일·기기 등록을 뒤지면 시간만
버린다. 개발자 포털에서 `Apple Development`만 폐기하면 풀린다(`Apple Distribution`은 두기).
**바이너리가 그대로면 `release` 대신 `submit` 레인을 쓴다** — 아카이브를 만들지 않아
인증서를 아예 쓰지 않는다. 자세한 내용은 `APP_STORE_RELEASE.md` 0절.

**2. iOS에서 `<img>`는 그 자체로 이미지 어포던스의 대상이다.**
스핀 버튼을 누르면 모서리 브래킷이 떴다 — WKWebView가 렌더 시점에 피사체를 분석하는
Live Text 계열 동작이다. `-webkit-touch-callout: none`은 콜아웃 메뉴만 막고 이건 못 막는다.
`pointer-events: none`으로 히트 테스트에서 빼도 **안 막혔고, 그게 원인을 특정한 단서였다**
(히트 테스트가 아니라 렌더 시점이라는 뜻). 장식용이고 탭 대상이 아니면 **CSS 배경으로
그리면** 분석 대상 자체가 없어진다.

**3. 저장소에는 릴리스 버전이 기록되지 않는다.**
`project.pbxproj`의 `MARKETING_VERSION = 1.0`은 자리표시자이고 fastlane이 워크플로
입력값으로 덮어쓴다. 코드만 보고 다음 버전을 짐작하면 틀린다 —
`APP_STORE_RELEASE.md` 0절 "버전 이력" 표나 App Store Connect를 본다.

**4. `deploy.yml`은 `main` push만 트리거한다.**
작업 브랜치에서 iOS를 dispatch하면 앱만 최신이 되고 **웹은 조용히 뒤처진다.**
이번에 3주치가 밀려 있었다. iOS를 브랜치에서 돌렸으면 웹 배포 상태를 함께 확인한다.

**5. 이 머신의 환경 제약.**
`gh`는 설치돼 있지만 **PATH에 없다** (`C:\Program Files\GitHub CLI\gh.exe`).
JDK·Android SDK·Android Studio는 **없다** — Android 빌드가 불가능하다.
PowerShell 실행 정책 때문에 `npm.ps1`이 막히므로 `npm.cmd`를 쓴다.

**6. 규정 오해 정정 — TourAPI 캐시는 "금지"가 아니라 "권고"다.**
공모전 FAQ 원문은 "실시간 호출 방식으로 활용하실 것을 **강력히 권고**"이고 사유도 규정이
아니라 데이터 동기화다. 다만 캐시를 쓰지 않기로 한 판단은 유지한다 — 예열 시점만 고쳐도
트래픽 문제가 사라졌고, 인증키 호출 이력 검증은 실제 규정이라 캐시가 그 이력을 줄인다.
근거는 `docs/competition.md` 핵심 규정 1번.
