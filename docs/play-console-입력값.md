# Google Play Console 입력값 — Spindle

> 작성 2026-08-16 · 갱신 2026-08-17 · 화면 순서대로 복사·붙여넣기 하면 되도록 정리한 문서입니다.
> 절차·일정·주의사항은 `../GOOGLE_PLAY_RELEASE.md`, 판단 근거는 `store-listing.md`를 보세요.
> **선행 조건**: ✅ 해소됨 (2026-08-17) — `https://spindle-6vp.pages.dev/privacy` 가 정책 본문을 200으로 반환합니다.

---

## 0. 앱 만들기

| 항목 | 값 |
|---|---|
| 앱 이름 | `Spindle` |
| 기본 언어 | 한국어 (대한민국) — `ko-KR` |
| 앱 또는 게임 | **앱** |
| 무료 또는 유료 | **무료** |
| 패키지명 | `kr.spindle.app` (AAB에 고정 — 변경 불가) |

---

## 1. 스토어 등록정보

### 앱 이름 (30자)

```
Spindle
```

### 간단한 설명 (80자) — 현재 33자

```
휴대폰을 돌려 만나는 부산 원도심·영도 방향 기반 관광 탐색
```

### 자세한 설명 (4,000자) — 현재 372자

```
어디로 갈지 망설여질 때, 휴대폰을 돌리거나 흔들어 보세요. Spindle이 나침반이 가리키는 방향에서 부산 원도심·영도의 관광지를 찾아 새로운 목적지를 제안합니다.

현재 위치와 방향은 기기 안에서만 계산됩니다. 나침반을 사용할 수 없는 환경이거나 부산 밖에 있어도 여행 모드에서 출발점을 직접 골라 같은 탐색을 즐길 수 있습니다.

한 곳만 고르는 대신 방향을 따라 이어지는 여행 코스를 살펴보고, 방문한 관광지에는 도장깨기 기록을 남길 수 있습니다. 마음에 든 결과는 공유 카드로 만들어 여행 동행이나 친구에게 보낼 수 있습니다.

익숙한 인기 명소만 반복해서 찾기보다, 휴대폰이 가리키는 방향을 따라 부산의 다음 장소를 발견해 보세요.
```

### 그래픽 자산 — 스크린샷은 App Store와 같은 세트 (2026-09-11)

| Play 항목 | 파일 | 규격 | 상태 |
|---|---|---|---|
| 앱 아이콘 | `fastlane/metadata/android/ko-KR/images/icon.png` | 512×512, 32-bit PNG(알파 필요) | ✅ RGBA |
| 그래픽 이미지 | `fastlane/metadata/android/ko-KR/images/featureGraphic.png` | 1024×500, 24-bit PNG(알파 금지) | ✅ 2026-09-11 교체 — 해 질 녘 부산 바다 + "어디 갈지 고민될 땐, 휴대폰만 돌리세요" (Codex 생성 배경 + Pretendard 합성, 시안 C) |
| 휴대전화 스크린샷 | `.../images/phoneScreenshots/` 5장 | 1442×2868, 24-bit PNG | ✅ |
| 7인치 태블릿 스크린샷 | `.../images/sevenInchScreenshots/` 5장 | 휴대전화와 같은 파일 | ✅ |
| 10인치 태블릿 스크린샷 | `.../images/tenInchScreenshots/` 5장 | 휴대전화와 같은 파일 | ✅ |

업로드 순서: `01_spin` → `02_map` → `03_home` → `04_stamp` → `05_settings`
(목록에서 앞 2장이 가장 많이 노출되므로 스핀·지도를 앞에 둡니다.)

**App Store 스크린샷(`fastlane/screenshots/ios/ko/`)과 같은 iPhone 덱에서 굽습니다.**
기기 틀·캡션·앱 화면이 같습니다. 가운데 1320px을 잘라 비교하면 색 차이가 평균 0.2, 최대 8 이하예요.
캔버스만 좌우 61px씩 넓습니다.

- **왜 넓히나**: App Store 규격 1320×2868은 Play 규정 "긴 변이 짧은 변의 2배를 넘을 수
  없다"(2868 > 2640)에 걸립니다. 1442×2868(1.99배)이면 통과합니다.
- **왜 이미지 가장자리를 복사해 붙이지 않나**: 배경의 눈금판 방위 글자(W·E)와 선이 뒤집혀
  보입니다. 대신 에디터에서 캔버스를 넓혀 배경을 새로 그립니다.
- **다시 만들기**: `tools/store-screenshots`에서 `npm run export:android`.
  휴대전화·태블릿 세 폴더를 한 번에 갈아 끼웁니다. 원본 `app-store-screenshots.json`은
  건드리지 않습니다(좌표를 옮긴 임시 사본을 씁니다). iPhone 덱을 고쳤다면 `export:ios`와
  함께 돌려 두 스토어 이미지를 맞춥니다.
- **태블릿 슬롯**: 예전에는 480px 폭 앱 양옆에 남색 여백이 찍힌 태블릿 캡처를 따로 올렸습니다.
  지금은 휴대전화와 같은 이미지를 씁니다. Play Console에서 기존 태블릿 이미지를 지우고 같은 5장을
  올리거나, 콘솔이 허용하면 태블릿 슬롯을 비워 둡니다.

### 분류

| 항목 | 값 |
|---|---|
| 앱 카테고리 | **여행 및 지역정보** |
| 태그 | 여행, 지도/내비게이션 성격 태그 중 해당하는 것 |
| 스토어 등록정보 연락처 | `enderpawar@gmail.com` |
| 외부 마케팅 | 해당 없음 |

---

## 2. 앱 콘텐츠 (필수 설문)

### 개인정보처리방침

```
https://spindle-6vp.pages.dev/privacy
```

> Cloudflare Pages가 `.html`을 벗겨 `/privacy`로 308 리다이렉트합니다.
> `.html`을 넣어도 결국 도달하지만, 심사에는 리다이렉트 없이 200을 주는 위 URL을 씁니다.
> 앱 내부에서는 이 URL로 이동하지 않고 `SettingsScreen`이 하단 시트로 같은 본문을 보여줍니다
> (본문 원본 `web/src/content/privacyPolicy.ts`). 위 공개 URL은 심사 제출용으로 계속 유지하며,
> 그 실체인 `web/public/privacy.html`은 삭제하지 않습니다.

### 앱 액세스 권한

> **"모든 기능을 특별한 액세스 권한 없이 사용할 수 있습니다"** 선택

근거: 로그인·계정·회원가입이 없고, URL 접속만으로 전 기능을 사용할 수 있습니다.

### 광고

> **"아니요, 앱에 광고가 없습니다"**

### 콘텐츠 등급 설문

이메일 입력 후 카테고리는 **참조 서비스 / 뉴스 또는 정보성 앱** 계열을 선택하고, 전 문항 **아니요**:

| 질문 영역 | 답변 |
|---|---|
| 폭력 / 성적 콘텐츠 / 욕설 / 도박 / 주류·담배·약물 / 공포 | 모두 **없음** |
| 사용자 생성 콘텐츠 | **없음** |
| 사용자 간 상호작용 | **없음** |
| 사용자 위치 공유 | **없음** — 위치를 다른 사용자나 서버로 공유하지 않습니다 |
| 디지털 상품 구매 | **없음** |

### 타겟층 및 콘텐츠

| 항목 | 값 |
|---|---|
| 타겟 연령대 | **18세 이상** (아동 타겟 아님) |
| 아동에게 어필하는 요소 | 없음 |
| Google Play 패밀리 정책 대상 | 아니요 |

### 데이터 안전 (Data Safety)

**핵심**: Spindle 자체는 좌표를 전송하지 않지만, **카카오맵 SDK가 지도 타일을 요청할 때 조회 지역이 카카오 서버로 전달**됩니다.
제3자 SDK의 수집까지 신고 대상으로 보는 Play 기준에 맞춰 아래처럼 답합니다. (판단 근거 전문: `store-listing.md` 2절)

| 데이터 유형 | 수집 | 공유 | 목적 | 비고 |
|---|---|---|---|---|
| 위치 — **대략적 위치** | **예** | 아니요 | **앱 기능** | 추적 아님. 저장하지 않으므로 삭제 요청 대상 아님 |
| 위치 — 정확한 위치 | 아니요 | 아니요 | — | 정확한 GPS 좌표는 단말 밖으로 나가지 않음 |
| 개인 정보(이름·이메일·전화번호) | 아니요 | 아니요 | — | 계정·로그인 없음 |
| 기기 또는 기타 식별자 | 아니요 | 아니요 | — | 분석·추적 SDK 미사용 |
| 앱 활동 / 사용자 생성 콘텐츠 | 아니요 | 아니요 | — | 방문 기록은 기기 내부에만 저장 |
| 금융 정보 / 구매 내역 | 아니요 | 아니요 | — | 광고·인앱결제 없음 |

추가 문항:

| 질문 | 답변 |
|---|---|
| 데이터가 전송 중 암호화되나요 | **예** (전 통신 HTTPS) |
| 사용자가 데이터 삭제를 요청할 수 있나요 | **아니요** — 서버에 사용자 데이터를 저장하지 않음 |

### 정부 앱 / 금융 기능 / 건강 앱

전부 **해당 없음**.

---

## 3. 비공개 테스트 출시

| 항목 | 값 |
|---|---|
| 업로드 파일 | **`web/android/spindle-1.0-vc2-release.aab`** (8,497,606 B, versionCode **2**, versionName 1.0) |
| 서명 확인 | `jarsigner -verify` → `jar verified.` (2026-08-17 재빌드본) |
| 출시 이름 | `Spindle 1.0 비공개 테스트` |

> ⚠ 같은 폴더의 `superseded-do-not-upload_spindle-1.0-vc1.aab`는 **올리지 마세요.**
> 8/14 빌드라 출처 표기 정리·설정 문구 변경·현 위치 출발점이 빠져 있어 웹 배포본과 어긋납니다.
> vc2는 `main` `c1d15bc` 기준으로 빌드해 웹(<https://spindle-6vp.pages.dev/>)과 내용이 같습니다.

### 출시 노트

```
Spindle 1.0 첫 비공개 테스트 버전입니다.
나침반 방향 기반 관광지 추천, 방향 기반 여행 코스, 혼잡 예측 명소 지도,
도장깨기와 공유 카드 기능을 테스트합니다.
```

> 기존 AAB를 그대로 사용합니다(2026-08-16 결정). 검증이 끝난 산출물이라 위험이 낮고 바로 올릴 수 있습니다.
> 테스트 중 피드백을 반영할 때는 versionCode를 `2`, `3`으로 올리고 **같은 릴리스 키로** 서명합니다.

### 국가/지역

대한민국 포함 (Doply 테스터 기기가 막히지 않도록 국가 제한을 좁히지 마세요).

---

## 4. 테스터 등록 (Doply)

절차는 `../GOOGLE_PLAY_RELEASE.md` 3절 B·D를 따릅니다. 요약:

1. Doply에 Spindle 등록 → 테스터 이메일 CSV 다운로드
2. Play Console `테스트 및 출시 > 테스트 > 비공개 테스트 > 트랙 관리 > 테스터`
3. 이메일 목록 `doply-spindle` 생성 후 CSV 업로드 → 트랙 테스터로 선택 → 저장
4. `테스터` 탭에서 참여 링크 복사 (`https://play.google.com/apps/testing/kr.spindle.app` 형태)
5. Doply에 링크 입력 → 설치 가능 기기 **12대 이상** 확인 → 그때만 결제(첫 이용 표시가 USD 6.99)
6. 테스트 시작

> ⚠ Doply에 **절대 주지 않는 것**: 키스토어(`spindle-release.jks`), `keystore.properties`,
> 키 비밀번호, Play Console 로그인·권한, Google Play Developer API 서비스 계정 JSON.
> AAB 업로드는 반드시 본인이 Play Console에서 직접 합니다.

### 테스트 기간 중 건드리면 안 되는 것

비공개 트랙 일시중지 · `doply-spindle` 목록 삭제/해제 · 참여 국가 제거 · 패키지명 변경 · 서명키 변경

---

## 5. 프로덕션 액세스 신청

12명 이상이 **14일 연속** opt-in한 뒤 신청합니다. 14일째보다 **15~16일째**에 확인 후 신청하는 편이 안전합니다.

신청서에 쓸 내용(실제 기록에 맞춰 채우세요):

| 항목 | 준비 |
|---|---|
| 테스터 모집 방식 | Doply 테스터 풀 + 지인 몇 명 |
| 사용한 주요 기능 | 방향 스핀(현장/여행 모드), 추천 결과 카드, 여행 코스, 명소 지도, 도장깨기, 공유 카드 |
| 받은 피드백 | 테스트 기간 중 Play Console 비공개 의견·별도 기록에서 정리 |
| 반영한 변경사항 | 피드백 반영 시 versionCode를 올려 업데이트한 내역 |
| 프로덕션 준비 근거 | 실기기에서 센서 권한 흐름·폴백까지 확인, 크래시 없음 |

> 지인 2~3명 이상에게 **위치 권한 허용/거부, 스핀, 센서 방향, POI 선택**을 실기기에서 직접 시켜보고
> 그 결과를 기록해 두세요. 신청서의 "받은 피드백"은 실제 내용이어야 합니다.

---

## 참고

- 절차 전체·일정 역산: `../GOOGLE_PLAY_RELEASE.md`
- 답변 판단 근거 원문: `store-listing.md`
- 개인정보처리방침 원본: `../web/public/privacy.html`
