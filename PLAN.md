# PLAN — 구현 Phase 정의

> 에이전트 실행 단위. **한 번에 한 phase만** 작업하고, 이전 phase의 DoD(완료 기준)가 체크되기 전에 다음 phase를 시작하지 않는다.
> 진입점: Claude Code는 `/phase N`, Codex는 `spindle-phase` 스킬 — 둘 다 아래 "실행 절차"를 그대로 따른다.
> 모든 phase에서 AGENTS.md 절대 원칙 6개가 우선한다.

## 실행 절차 (모든 에이전트 공통)

Phase N을 실행할 때 순서대로:

1. **범위 확인** — 이 문서의 Phase N 절을 읽는다. 거기 적힌 작업 항목이 범위의 전부다. **다음 phase 기능을 미리 만들지 않는다.** 선행 phase(0..N-1)에 미체크 DoD가 있으면 구현을 시작하지 말고, 무엇이 막혀 있는지(특히 Phase 0 사람 작업이 원인인지) 보고하고 중단한다.
2. **규약 로드** — AGENTS.md "구현 규약 문서" 표에서 Phase N 작업 영역에 해당하는 문서를 먼저 읽는다. 화면 작업이면 `docs/ui.md`, 데이터 모듈 작업이면 `docs/zones.md`·`docs/curation.md` 추가.
3. **구현** — AGENTS.md 절대 원칙 6개 우선; 위반이 의심되는 요구가 이 문서에 있으면 구현하지 말고 보고. ui.md의 ⚠(미확정 제안) 항목은 제안값대로 구현하되 완료 보고에 명시. zones/curation 미확정 값은 임시값 + `// TODO(zones.md)` 주석 (Phase 5부터 확정값 필수).
4. **DoD 검증** — Phase N의 DoD 각 항목을 실제로 검증한다 (테스트 실행, 금지 문자열 grep, dev 서버 확인). **"구현했으니 됐다"로 체크하지 않는다.** "사람 검증" 항목은 자동 체크하지 말고 사용자용 체크리스트로 보고에 정리한다.
5. **마무리** — 이 문서의 DoD 체크박스·진행 상태를 갱신한다 (사람 검증 항목은 미체크 유지). Phase 1에서 명령어가 확정되면 AGENTS.md "명령어" 섹션도 갱신. git 저장소면 phase 단위로 커밋. 완료 보고 형식: 구현 요약 / DoD 항목별 검증 근거 / 사람 확인 필요 항목 / 다음 phase 안내.

인자 없이 또는 `status`로 호출되면: 진행 상태 체크박스를 읽어 완료/미완료와 다음 phase만 보고한다.

## 진행 상태

- [x] Phase 0 — 사전 준비 (사람) — 사용자 완료 확인(2026-08-02)
- [x] Phase 1 — 스캐폴딩 + 프록시 — 사용자 실검증 완료 확인(2026-08-02)
- [x] Phase 2 — 여행 모드 코어 루프 (추천 엔진) — 사용자 실검증 완료 확인(2026-08-02)
- [x] Phase 3 — 결과 카드 — 사용자 실검증 완료 확인(2026-08-02)
- [x] Phase 4 — 현장 모드 (센서) — 사용자 실기기 검증 완료 확인(2026-08-02)
- [x] Phase 5 — 콘텐츠·공유 카드·폴리시 — 사용자 실기기 검증 완료 확인(2026-08-02)
- [x] Phase 6 — PWA 완성 + 프로덕션 배포 — 사용자 배포·실검증 완료 확인(2026-08-02)
- [ ] Phase 7 — 방향 기반 여행 코스 + 2순위 기능 (조건부)
- [ ] Phase 8 — 제출 준비

---

## Phase 0 — 사전 준비 (사람 작업, 에이전트 구현 불가)

| 항목 | 차단 관계 |
|---|---|
| [x] TourAPI **운영계정** 신청·인증키 발급 (2026-07-08 실호출 확인) | ~~Phase 1 차단~~ — 해소 |
| [x] 사무국 질의 3건 발송 (docs/competition.md) — 사용자 완료 확인(2026-08-02) | 개발 비차단 |
| [x] Cloudflare 계정 준비 (2026-07-31 Wrangler OAuth 로그인·Pages 쓰기 권한 확인) | ~~Phase 6 차단~~ — 해소 |
| [x] `docs/zones.md` 존 다각형·보정거리 확정 — 사용자 완료 확인(2026-08-02) | Phase 5 진입 조건 충족 |
| [x] `docs/curation.md` POI 20곳 이상 티어 지정 — 사용자 완료 확인(2026-08-02) | Phase 5 진입 조건 충족 |
| [x] 프로젝트 OneDrive 밖 이동 + `git init` 여부 결정 — 현재 경로 유지·기존 git 저장소 사용으로 결정(2026-08-02) | Phase 1 확인 완료 |

## Phase 1 — 스캐폴딩 + 프록시

**목표**: 프록시 경유 TourAPI 호출이 성립하는 빈 앱.

- 모노레포 구성: `web/`(Vite + React + TypeScript + PWA 플러그인), `proxy/`(Cloudflare Workers, wrangler)
- 프록시: 파라미터 화이트리스트, 키 주입, CORS 설정, 로컬 dev 환경(`wrangler dev`) — `tourapi` 스킬 규약 준수
- `areaCode2`로 중·동·서·영도구 시군구코드 확인 → 상수 모듈에 코드와 확인 날짜 기록
- `areaBasedList2` 4개 구 호출 유틸 + 페이징 + 세션 메모리 캐시
- **품질 게이트 셋업**: vitest + eslint + `tsc --noEmit`을 묶은 `npm run check` 스크립트 정의(기존 `scripts/guard.mjs` 금지 패턴 스캔 포함), `git config core.hooksPath .githooks`로 pre-commit hook 활성화 (git init 선행 — Phase 0 확인)
- AGENTS.md "명령어" 섹션 채우기 (dev/build/check/프록시 실행)

**DoD**
- [x] `web` dev 서버에서 4개 구 POI 목록이 프록시 경유로 화면에 렌더링 — **키 확보(2026-07-08)로 차단 해소**: `areaCode2`(부산 시군구 16개)·`areaBasedList2`(중54·동44·서26·영도42) 실호출 확인, `regionCodes.ts` 시군구코드 areaCode2로 재확인 완료. 사용자 브라우저 실검증 완료 확인(2026-08-02)
- [x] `web/` 빌드 산출물·소스에서 인증키 문자열 검색 → 0건 — **실제 키 문자열로 `web/src`·`web/dist` 재검색 0건, serviceKey 패턴도 0건 (2026-07-08)**
- [x] 화이트리스트 외 파라미터(예: mapX) 전달 시 프록시가 400 반환 (2026-07-07 wrangler dev + curl로 확인, `proxy/test/validate.test.ts`로도 고정)
- [x] `npm run check` 통과 + **금지 패턴이 든 파일을 커밋 시도하면 pre-commit이 실제로 거부함을 확인** (위반 파일로 리허설 후 제거, 2026-07-07)

## Phase 2 — 여행 모드 코어 루프 (추천 엔진)

**목표**: 데스크톱 브라우저에서 스핀 → 추천 → 결과(텍스트)까지 완주. `algorithm` 스킬 규약 준수.

- 존 데이터 모듈: `docs/zones.md` → TS 상수 (미확정 값은 `// TODO(zones.md)` 표기, 임시값으로 동작은 가능하게)
- 큐레이션 모듈: `docs/curation.md` → TS 상수 (없는 contentId는 T2)
- 방위 수학 유틸(8방위 분류·원형 경계·중심선 가중) + 점수 엔진(방향×접근×운영×분산) + 상위 3 가중 랜덤(RNG 주입형) + 인접 방위 확장
- 이동시간 다이얼(가볍게/반나절/하루), 출발점 선택(부산역·남포동·영도 프리셋 3개, 지도 픽은 Phase 5로 이연 가능)
- 화면 스핀 제스처: 드래그 → 관성 감속 → 정지 방위각 산출 (연출 각도 = 알고리즘 입력 각도 동일 보장)

**DoD**
- [x] 여행 모드 완주: 출발점 선택 → 다이얼 → 스핀 → 추천 결과 표시 — **점수 엔진 배선 완료(2026-07-09)**: `engine/spinRecommend.ts`가 `engine/recommend`(방향×접근(존-교량)×운영×분산)를 출발점·다이얼과 함께 호출하도록 `App.tsx` 연결(기존 목 추천 제거). 출발점·다이얼이 결과를 실제로 바꿈을 `spinRecommend.test.ts`로 고정. 사용자 브라우저 실검증 완료 확인(2026-08-02)
- [x] 유닛테스트 통과: 방위 0°/360° 경계, 바다 방향(남포→남) 인접 확장 + 사유 문자열, 다이얼 임계 경계값(20/40분), 시드 고정 가중 랜덤 재현성 (web 26개 테스트, 2026-07-07) + 배선 테스트 5개(출발점 민감도·다이얼 제약·재현성·전 조합 후보≥1, 2026-07-09)
- [x] 후보 0개 방위에서 확장 사유가 UI에 노출 — 엔진 `expansionReason`을 브릿지가 `expandReason`으로 매핑 → RevealScreen 노출. 전 방위·출발점·다이얼 조합에서 후보≥1 보장(데드엔드 방지 폴백)까지 테스트로 고정. 사용자 화면 실검증 완료 확인(2026-08-02)

## Phase 3 — 결과 카드

**목표**: 추천 결과가 제출 수준의 카드로. `docs/ui.md` S4 명세 준수.

- `detailCommon2`/`detailIntro2`/`detailImage2` 연동 (결과 시점 호출)
- 카드 UI: 이미지(폴백 = 방위 색상 배경 + 아이콘), "이곳의 매력" 소개, 보정 도보시간, 유형별 방문정보 최대 4행(파싱 실패 시 원문 노출). 인기도 티어는 추천 계산에만 사용하고 배지로 표시하지 않음
- 카카오맵 길찾기 딥링크(선택 모달 없이 바로 연결) / "다른 후보 보기"(나머지 2개 순환) / "다시 돌리기"
- API 장애·타임아웃 에러 UI (빈 화면 금지)

**DoD**
- [x] 결과 카드가 ui.md S4 요소를 전부 포함 — 사용자 브라우저 실검증 완료 확인(2026-08-02)
- [x] 이미지 없는 POI에서 폴백 정상 표시 — 사용자 육안 검증 완료 확인(2026-08-02)
- [x] 프록시 강제 차단 상태에서 에러 UI + 재시도 동작 — 사용자 라이브 차단 검증 완료 확인(2026-08-02)

**Phase 3 사람 체크리스트 (브라우저 — 심사 시연 대상)**
- [x] 루트 앱 실데이터: 스핀 → S4 카드에 이미지·소개·도보시간·운영상태 표시 — 사용자 확인(2026-08-02)
- [x] 이미지 없는 POI(예: 국제시장)에서 방위색 폴백 카드 표시 — 사용자 확인(2026-08-02)
- [x] `?demo=1`(센서·네트워크 불필요)로도 카드 완주 — 사용자 확인(2026-08-02)
- [x] 프록시 중지 상태에서 에러 UI + [재시도] 동작 — 사용자 확인(2026-08-02)
- [x] 결과·코스 화면의 길찾기가 선택 모달 없이 카카오맵 딥링크로 바로 열림 — 자동 테스트·사용자 요청 반영(2026-08-02)

## Phase 4 — 현장 모드 (센서)

**목표**: 실기기에서 GPS + 나침반으로 동작. `sensors` 스킬 규약 준수.

- iOS 권한 요청 플로우(버튼 제스처 내), `webkitCompassHeading`/`deviceorientationabsolute` 분기, 원형 평균 스무딩
- GPS 1회 취득 → 존 판정 (전부 단말 내)
- 권한 거부·미지원·타임아웃 → 여행 모드 폴백 UI

**DoD (자동 검증)**
- [x] 센서 미지원 환경(데스크톱)에서 폴백이 자연스럽게 여행 모드로 유도 — `selectMode("field")`가 `isFieldModeAvailable()`(DeviceOrientation+Geolocation) 미충족 시 폴백 카드("여행 모드로 계속" primary)로 유도, 권한/GPS 실패도 동일 폴백. 미지원 신호 프리미티브 `sensors/support.test.ts`(6)로 고정(`getCurrentFix`→unsupported/denied, `requestOrientationPermission`→unsupported/granted/denied). 시각적 "자연스러움"은 사람 체크리스트
- [x] 네트워크 요청 페이로드에 좌표·방위각 미포함 (테스트로 검증) — 현장 모드 데이터 경로(`areaBasedList2`·`detail*`)의 모든 요청 URL에 좌표·방위각·좌표 파라미터가 없음을 `sensors/netSafety.test.ts`로 고정. 좌표·방위각은 순수 `recommend()`로만 소비(네트워크 미호출). guard가 클라이언트 내 좌표 파라미터도 차단

**DoD (사람 검증 — 완료 보고에 체크리스트로 명시할 것)**
- [x] iPhone Safari: 권한 허용/거부 두 경로, 8방위 정확도 — 사용자 실기기 확인(2026-08-02)
- [x] Android Chrome: heading 취득, 편차 ±15° 이내 — 사용자 실기기 확인(2026-08-02)

**Phase 4 사람 체크리스트 (실기기 — sensors 스킬 검증 항목)**
- [x] iPhone Safari: "내 위치에서 돌리기" → 권한 프롬프트 → 허용 → 원판이 기기 방위 추종 → 멈추면 자동 잠금 → S4 카드 — 사용자 확인(2026-08-02)
- [x] iPhone Safari: 권한 거부 → 폴백 카드 → "여행 모드로 계속" 동작 — 사용자 확인(2026-08-02)
- [x] Android Chrome: `deviceorientationabsolute` heading 취득, 8방위 편차 ±15° 이내 — 사용자 확인(2026-08-02)
- [x] 데스크톱: 현장 모드 선택 시 즉시 폴백 카드 표시(센서 미지원 안내) + 여행 모드 복귀 — 사용자 확인(2026-08-02)
- [x] "이 방향으로 결정"(수동 잠금)과 자동 잠금 모두 같은 방위로 결과 생성 — 사용자 확인(2026-08-02)

## Phase 5 — 콘텐츠·공유 카드·폴리시

**목표**: "기능이 있다"에서 "완성돼 보인다"로. **시작 전 zones/curation 확정값 반영 (Phase 0 확인).**

- 8방위 큐레이션 메시지 8종 작성 (ui.md 카피 가이드 준수, 예시: 동 = "시작과 새벽의 방향, 떠오르는 해를 만나러")
- 공유 이미지 카드: Canvas로 인스타 스토리 비율(1080×1920) PNG 생성, Web Share API + 다운로드 폴백
- 온보딩(첫 방문 1~2장), 전 화면 로딩·에러·빈 상태 정비, 스핀 연출 폴리싱
- 출발점 지도 픽 (Phase 2에서 이연했다면)

**DoD**
- [x] 8방위 메시지가 스핀 정지 연출에 표시 — 루트 앱의 별이 화면 흐름 기준으로 유지. 콜드 스타트 하단의 굽이치는 2겹 파란 물결이 대기 중 찰랑이고 완료 시 살짝 투명해지며 하단 공백 없이 화면을 채우는 1회 모션과 모션 최소화 페이드를 추가. `npm run check`·`npm run build`·guard 통과(2026-07-31)
- [x] 공유 카드 PNG가 모바일에서 저장/공유 가능 — 사용자 모바일 실기기 확인(2026-08-02)
- [x] 모든 API 호출 지점에 로딩 상태, 모든 실패 경로에 에러 UI 존재 — 전수 점검(2026-07-08): POI 목록 `usePois` 로딩/에러+재시도, 상세 `useDetail` 스켈레톤/부분 실패+재시도, 공유 카드 생성 실패 notice, 후보 0개 빈 상태 안내, 현장 센서 권한/GPS 실패 폴백. `npm run check` 통과

**Phase 5 사람 체크리스트 (모바일/브라우저)**
- [x] iPhone/Android: S5 공유하기(Web Share API)로 PNG 공유 — 사용자 확인(2026-08-02)
- [x] iPhone/Android: S5 저장 버튼으로 PNG 다운로드/갤러리 저장 확인 — 사용자 확인(2026-08-02)
- [x] 루트 앱: 온보딩 → 출발점 선택 → 스핀 → S3 메시지 → S4 → S5 저장까지 육안 확인 — 사용자 확인(2026-08-02)

## Phase 6 — PWA 완성 + 프로덕션 배포

**목표**: 심사 제출용 고정 URL. `pwa` 스킬 규약 준수.

- manifest(standalone·portrait·마스커블 아이콘), SW(정적 자산만 프리캐시, **API 경로 network-only**), 오프라인 안내
- 설치 유도: Android `beforeinstallprompt` 배너, iOS 공유 시트 안내
- SW 업데이트 자동 반영(skipWaiting + 리로드 토스트)
- Cloudflare Pages(프로덕션 브랜치 고정) + Workers 배포, 프록시 rate limit
- 운영계정 키를 프로덕션 프록시 환경변수에 설정 → **이후 모든 개발·테스트도 프로덕션 프록시 경유** (호출 이력 축적)

**DoD**
- [x] Lighthouse PWA installable 통과 — 사용자 배포 URL 실측 완료 확인(2026-08-02)
- [x] 시크릿 창 + 프로덕션 URL에서 여행 모드 완주 — 사용자 프로덕션 검증 완료 확인(2026-08-02)
- [x] SW가 API 응답을 Cache Storage에 저장하지 않음 (검증 테스트) — `vite.config.ts` `/api` runtime route를 `NetworkOnly`로 명시, `scripts/verify-pwa.mjs`가 빌드된 `dist/sw.js`에서 `/api` NetworkOnly와 TourAPI 엔드포인트 미포함을 검증. `npm run pwa:verify` 통과(2026-07-08)
- [x] 오프라인 상태에서 셸 표시 + "네트워크 필요" 안내 (추천 동작하면 규정 위반 신호) — `dist/sw.js`가 루트 앱 셸을 precache함을 `pwa:verify`로 검증, `cachePolicy.test.ts`가 오프라인 추천 차단을 고정

**Phase 6 사람 체크리스트 (브라우저/배포)**
- [x] Chrome Lighthouse: PWA installable 통과 — 사용자 확인(2026-08-02)
- [x] Chrome DevTools Offline: 셸 표시 + 추천 버튼 비활성 + 네트워크 필요 안내 — 사용자 확인(2026-08-02)
- [x] Android Chrome: 설치 배너 → 홈 화면 설치 — 사용자 확인(2026-08-02)
- [x] iPhone Safari: 공유 시트 "홈 화면에 추가" 안내 확인 — 사용자 확인(2026-08-02)
- [x] Cloudflare Pages + Workers 프로덕션 배포, 운영계정 키 환경변수 설정 — `https://spindle-6vp.pages.dev` + `https://spindle-proxy.enderpawar.workers.dev`, TourAPI `resultCode=0000`·CORS·`no-store` 확인 (2026-07-31)
- [x] 시크릿 창 + 프로덕션 URL에서 여행 모드 완주 — 사용자 확인(2026-08-02)

## Phase 7 — 방향 기반 여행 코스 + 2순위 기능 (Phase 1–6 DoD 전부 체크 후에만, 시간 부족 시 스킵)

- [x] **방향 기반 여행 코스 (최우선)**: 단일 추천 결과의 `[이 방향으로 코스 짜기]`에서 `docs/course.md` 규칙에 따라 2~4개 POI 코스 구성. 후보 선정·이동비용·순서 계산은 전부 단말 내 수행하고, 외부 경로 API는 사용하지 않음 — `engine/course.ts` 순수 엔진, 장소별 길찾기 딥링크만 제공. `npm --prefix web run test`·typecheck·guard 통과(2026-07-08)
- [x] **스핀 코스 모드 + 단말 내 탐험 안내**: 스핀 화면에서 `한 곳/코스`를 고르고, 코스 모드는 한 번의 확정 방위로 2~4곳을 구성해 미리보기 후 시작한다. 활성 안내 중에만 GPS·나침반을 구독해 직진/좌/우/뒤쪽과 도착을 단말에서 판정하며, 실제 도로 경로는 목적지 좌표만 담은 카카오맵 딥링크로 보완한다. 음성 인식은 사용하지 않고 `다음 경로` 버튼으로 진행한다 — 엔진·센서 수명주기·미리보기/안내/도착/완주·`?demo=1` 구현, `npm run check`·build 통과(2026-08-02)
- [x] 테마 덱: 바다/골목·시장/근현대·역사/야간/먹거리 — `engine/themes.ts`(category→테마 매핑 + POI별 큐레이션 태그), `ThemeDeckScreen` + 홈 "테마로 떠나기" 진입. **정본=screens 앱**(index.html)에 구현. `themes.test.ts` 5개, typecheck·guard 통과(2026-07-08). 브라우저 육안 확인은 사람 체크리스트
- [x] 축제 특별 카드: `searchFestival2`, 방위+기간 일치 시에만 일반 결과 위에 표시 — `engine/festival.ts`(방위→구 매핑·기간 판정·매칭) + `api/festivals.ts`(프록시 경유·세션 캐시·좌표 무전송) → `ResultScreen` 결과 위 특별 배너 + `FestivalScreen`(홈 축제 메뉴, 로딩/에러/빈 상태). `festival.test.ts` 7 + `festivals.test.ts` 3(요청에 좌표·방위각 없음 고정), typecheck·guard 통과(2026-07-08). **실데이터 표시는 진행 중 축제 + 프록시 필요**(사람 체크리스트)
- [x] 부산 동네 도장깨기: 방문 contentId 단말 저장, 존별 수집 현황 UI (좌표·개인정보 서버 전송 없음) — `lib/visited.ts`(localStorage·무전송, useSyncExternalStore) → StampScreen/HomeScreen 실시간 반영, 길찾기 시트 내 도장 획득 인라인 안내. `visited.test.ts` 5개, guard 통과(2026-07-08)
- [x] 관광지 혼잡 예측: `TatsCnctrRateService/tatsCnctrRatedList`를 고정 지역코드로 프록시 호출하고, 당일 예상 집중률 70 이상인 명소에 `오늘 혼잡 예상`, 당일 최고 예측값 40 이하인 명소에 `오늘 가기 좋아요` 표시. 추천 점수·후보 제외에는 사용하지 않으며 응답은 세션 메모리에만 보관 — API·보수적 명칭 매칭·지도/카드/리스트·상태 UI 구현, 자동 검증 완료(2026-08-02)
- [x] 테마 스핀 여행: 테마 덱에서 `이 테마로 돌리기`를 선택하면 8방위 디스크가 해당 테마 후보만 사용해 가중 랜덤 추천. 같은 테마 안에서만 방향 확장하며 결과마다 랜덤 여행 미션과 이동시간별 1~4개 장면 진행을 제공 — 엔진·테마 디스크·스핀 상태·결과 미션·연속 스핀 구현, 전체 check·build 통과(2026-08-02)
- [x] 오늘의 한 칸 미션: 홈의 `오늘의 스핀 추천` 아래에서 단말 날짜별 미션 1개를 보여주고 `[스핀하러 가기]`로 기존 스핀 화면에 연결. 완료 상태·API 호출·영속 저장은 추가하지 않음 — 날짜 순환 순수 모듈·홈 카드·출처 표기 구현, `npm run check`·`npm run build` 통과(2026-08-02)
- [x] 카카오맵 코스 길찾기 위임: S6의 1→2→3→4 코스 순서를 공개 POI 좌표만 담은 공식 `/link/by/walk/` URL로 열기. 별도 REST 토큰은 필요 없으며 사용자 GPS·방위각·선택 출발점은 링크에 포함하지 않는다 — `lib/mapLinks.ts` `kakaoMapCourseWalkUrl()` + `CourseScreen` 보조 CTA, 좌표 누락 제외·2곳 미만 CTA 숨김 구현. `mapLinks.test.ts` 코스 7개(도보 이동수단·순서·인코딩·2/4곳 경계·좌표 누락 제외·2곳 미만 폴백·사용자 위치 미포함), `npm run check`·`npm run build` 통과(2026-08-02). 기준은 `docs/course.md` 7장
- [ ] 익명 지표 카운터: Workers KV에 방위·티어별 추천/선택 카운트만 → 기능설명서 발전성 수치용 — **보류(사람 판단 필요)**: 방위 버킷을 서버로 전송하는 것이 AGENTS.md 절대 원칙 1(방위각 무전송)의 취지와 충돌 소지 + Phase 0 Cloudflare 계정 미준비로 배포·검증 불가. 규정 판단(competition.md/사무국) 확정 후 구현할 것

**테마 스핀 여행 DoD**

- [x] 바다/골목·시장/근현대·역사/야간/먹거리 테마를 선택해 스핀하고 결과까지 완주하며, 결과 후보가 모두 선택 테마에 속함 — 전 테마×출발점×8방위 테스트 통과(2026-08-02)
- [x] 선택 방향에 테마 후보가 없으면 같은 테마의 인접 방향과 전방위 순으로만 확장하고 이유를 노출하며, 다른 테마 장소를 조용히 섞지 않음 — 먹거리 20분 폴백·테마 보존 테스트 통과(2026-08-02)
- [x] 결과에 테마별 랜덤 미션과 `현재 장면/전체 장면` 진행을 표시하고, 마지막 전에는 `같은 테마로 다음 스핀`, 마지막에는 `테마 여정 마치기` 제공 — ThemeJourney 상태와 결과 CTA 연결·타입검사 통과(2026-08-02)
- [x] 테마 없음 기존 스핀 결과·재현성·다이얼 경계가 회귀하지 않고, RNG 고정·전 테마/출발점/방위 조합 테스트와 guard·build 통과 — web 155개·proxy 12개 테스트, guard·build 통과(2026-08-02)
- [ ] 280~480px에서 테마 덱·스핀·결과 화면 가로 오버플로 없이 CTA와 44px 터치 타겟을 사람 검증

**관광지 혼잡 예측 DoD**

- [x] 프록시가 별도 혼잡 예측 서비스로 라우팅하되 엔드포인트별 파라미터 화이트리스트를 적용하고 좌표·방위각 파라미터를 거부 — proxy 12개 테스트 통과(2026-08-02)
- [x] 명소 지도·카드·리스트에서 당일 예상 집중률 70 이상인 매칭 POI는 `오늘 혼잡 예상`, 당일 최고 예측값 40 이하인 매칭 POI는 `오늘 가기 좋아요`를 같은 의미의 아이콘·문구로 표시하고, 로딩·실패·재시도 상태에서도 기본 지도 탐색이 동작 — 경계·중복 예측 유닛테스트와 기존 mock/실패 상태 검증(2026-08-02)
- [x] 예측값은 추천 엔진 입력이 아니며 API 응답이 영속 저장소나 service worker 캐시에 남지 않음을 테스트·guard로 검증 — 세션 메모리 캐시·실패 제거 테스트, `npm run check`·`npm run pwa:verify` 통과(2026-08-02)
- [ ] 관광지 집중률 예측 API 별도 활용신청 후 프로덕션 키로 4개 구 실데이터·POI명 매칭을 사람 검증

**오늘의 한 칸 미션 DoD**

- [x] 같은 로컬 날짜에는 같은 미션, 다음 날짜에는 다음 미션이 선택되고 7일 뒤 순환함을 유닛테스트로 검증 — `dailyMissions.test.ts` 3개 통과(2026-08-02)
- [ ] 홈 추천 목록 아래에 미션 카드와 `출처: ⓒ한국관광공사`가 표시되고, 카드 선택 시 기존 스핀 화면으로 이동 — 구현·빌드 완료, 브라우저 런타임 미제공으로 육안 확인 필요
- [ ] 280·390·480px에서 가로 오버플로·문구 겹침·CTA 줄바꿈이 없고 모션 최소화 설정을 존중 — 반응형 CSS·전역 reduced-motion 규칙 적용, 브라우저 육안 확인 필요

**카카오맵 코스 길찾기 위임 DoD**

- [x] S6 하단 보조 CTA가 확정 코스의 방문 순서를 그대로 담은 `/link/by/walk/` URL을 새 컨텍스트로 열고, 기본 CTA는 `코스 안내 시작`으로 유지 — `CourseScreen` 링크 구현·`npm run build` 통과(2026-08-02)
- [x] URL에 공개 관광지 이름·좌표·순서만 담기고 사용자 GPS·방위각·정확도·demo 가상 위치·선택 출발점 좌표가 포함되지 않음 — 좌표 토큰 수 고정 테스트·guard 통과(2026-08-02)
- [x] 현재 위치→1번 장소는 목적지 전용 링크로 먼저 안내하고, 전체 코스 보기는 1번 장소부터의 순서로 분리. 1번 좌표가 없으면 2번으로 건너뛰지 않음 — `kakaoMapFirstStopDirectionsUrl()` 및 단위 테스트 통과(2026-08-02)
- [x] 한글·특수문자 장소명 인코딩, 2곳·4곳 경계, 좌표 누락 POI 제외, 유효 2곳 미만 시 CTA 숨김을 유닛테스트로 고정 — `mapLinks.test.ts` 통과(2026-08-02)
- [x] Kakao Map REST API·Navi SDK·Spindle 프록시를 호출하지 않는 URL 연결만 사용 — 순수 문자열 빌더, 네트워크 호출 없음(2026-08-02)
- [ ] 모바일 카카오맵(앱·모바일웹)과 PC 웹에서 코스 순서대로 도보 경로가 열리고, 팝업 차단·앱 미설치 시 장소별 [카카오맵 길찾기]로 계속 진행 가능함을 사람 검증

**방향 기반 여행 코스 DoD**

- [ ] 여행 모드에서 출발점 선택 → 스핀 → 단일 추천 → 2~4개 코스 표시까지 완주 — 루트 앱 기준으로 브라우저 육안 확인 필요
- [ ] 코스 모드에서 한 번 스핀 → 2~4곳 미리보기 → 명시적 안내 시작 → 장소별 도착 → `다음 경로` → 완주까지 진행하며, 테마 여정과 동시에 활성화되지 않음
- [x] 안내 엔진 유닛테스트 통과: 0/360 경계, 직진·좌·우·뒤쪽 분류와 8° 히스테리시스, 정확도 80m 이하 표본만 사용, 50m 이내 2회 연속 도착 판정 — demo 미터 이동 2개 포함 `courseGuidance.test.ts` 7개 통과(2026-08-02)
- [x] GPS·방위각·코스 배열이 네트워크·로그·영속 저장소로 나가지 않고, 위치 구독·나침반·Wake Lock이 일시정지·숨김·오류·완주·언마운트 때 해제됨 — 단말 전용 effect cleanup·`geolocation.test.ts`·guard 통과(2026-08-02)
- [ ] 센서 거부·미지원·서울 등 권역 밖에서도 빈 화면 없이 카카오맵 길찾기와 `?demo=1` 수동 시연 흐름을 사용할 수 있음
- [ ] 활성 안내가 전체화면 카카오맵(실패 시 내장 지도), 현재 위치 마커, 다음 목적지, 활성 구간선, 화면 절반 하단 시트로 표시되고 지도 팬·줌이 유지됨
- [ ] `?demo=1` GPS 조이스틱으로 가상 현재 위치·방위·거리·화살표가 함께 바뀌고 지도 카메라가 현재 위치를 포커스하며, 도착·다음 경로·완주까지 실제 안내 엔진으로 진행됨
- [x] 유닛테스트 통과: 첫 장소 방위 고정, 20/40분 이동예산 경계, 후보 부족 시 축소 코스, 동일 입력의 방문 순서 재현성 — `course.test.ts` 9개 추가, web 총 78개 테스트 통과(2026-07-08)
- [x] 후보가 2개 미만이면 단일 추천 화면을 유지하고 코스 생성 불가 사유를 표시 — `buildCourse()`가 `unavailable` 반환, S4에 "이 방향에서는 코스를 만들 장소가 부족해요" 안내 표시
- [x] 네트워크 요청에 사용자 좌표·방위각이 없고, 코스 후보와 API 응답이 영속 저장소에 남지 않음을 검증 — 코스 생성은 `engine/course.ts` 순수 함수, S6 상세는 기존 `fetchPoiDetailCached(contentId)`만 사용(좌표·방위각 미전송), `node scripts/guard.mjs` 위반 없음(2026-07-08)

## Phase 8 — 제출 준비

- `/submission-check` 전 항목 실행 → 미통과 항목 수정
- 기능설명서 초안 생성: 서비스 개요, 존 지도·가중치 표(docs/), API 활용 내역, 아키텍처(좌표 무전송), **범위 구체화 경위 문장**(SPEC 1.1), 발전성(백로그 + Phase 7 지표)
- 시연 영상 스크립트(여행 모드 기준 90초 구성안)
- 사무국 질의 회신 반영 확인 (competition.md 상태 표)

**DoD**
- [ ] submission-check 전 항목 통과
- [ ] 기능설명서 초안 완성 (최종 편집·제출은 사람)
- [ ] 마감(09-21 16:00) 최소 1일 전 제출 — **사람 최종 액션**
