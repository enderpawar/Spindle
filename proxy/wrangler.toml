# Spindle TourAPI 프록시 — 키 주입·요청 중계만 수행, 로그 미저장 (AGENTS.md)
# 로컬 개발: proxy/.dev.vars 파일에 TOURAPI_SERVICE_KEY=<디코딩된 키> 작성 (gitignore 대상)
# 프로덕션: wrangler secret put TOURAPI_SERVICE_KEY (docs/deploy.md 참고, GitHub Actions 아님 — 로컬 1회)
name = "spindle-proxy"
main = "src/index.ts"
# 로컬 개발용 wrangler(package-lock 고정 버전)의 workerd가 지원하는 최신 날짜에 맞춘다.
# 이보다 미래 날짜를 쓰면 `wrangler dev`가 기동에 실패한다(배포는 되지만 로컬이 깨짐).
# wrangler를 올릴 때 이 날짜도 함께 올릴 것.
compatibility_date = "2026-07-08"

# 프로덕션 CORS 허용 오리진 — Pages 프로덕션 도메인만 허용한다.
# 커스텀 도메인으로 전환하면 이 값도 함께 바꾸고 프록시를 다시 배포할 것.
# docs/deploy.md "5. ALLOWED_ORIGIN 갱신" 절 참고.
[vars]
ALLOWED_ORIGIN = "https://spindle-6vp.pages.dev"
