# Spindle Google Play 출시 인계 문서

> 다음 세션에서 Google Play 등록 또는 Doply 비공개 테스트를 진행하기 전에 이 문서를 먼저 읽는다.
>
> 마지막 갱신: 2026-08-25 (Asia/Seoul)

## 0. 지금 상황 요약 (2026-08-17)

**개발자 계정 인증 완료. 개인정보처리방침 배포 완료. 선행 블로커 없음.**
이제 앱 생성 → 비공개 테스트 → 프로덕션 액세스 순으로 바로 진행할 수 있다.

### 해소된 블로커 — privacy.html 프로덕션 배포 (2026-08-17)

원인은 `privacy.html`이 든 커밋(`0828094`)이 `origin/main`에 푸시되지 않아 CI가 돌지 않은 것이었다.
로컬 `main`이 `origin/main`보다 5커밋 앞서 있었다. `git push origin main:main`으로 해소했다.

| | |
|---|---|
| 배포 실행 | 2026-08-17, GitHub Actions run `31993780857` — check·web·proxy 3잡 전부 성공 |
| 배포된 `main` | `4eaa7a5` (`95ddb8b..4eaa7a5`, 5커밋) |
| 검증 | `https://spindle-6vp.pages.dev/privacy` → 200, 6,275B, 정책 본문 확인 |
| 배포 방식 | `main`에 push → GitHub Actions(`.github/workflows/deploy.yml`)가 자동 배포 |

**⚠ Play Console에 넣을 URL은 `https://spindle-6vp.pages.dev/privacy` (`.html` 없이).**
Cloudflare Pages가 `.html`을 벗겨 `/privacy`로 308 리다이렉트한다. `.html`도 결국 도달하지만
심사에는 리다이렉트 없이 200을 주는 쪽을 쓴다. 앱 내부 링크(`SettingsScreen`)는 `/privacy.html`
그대로 유지한다 — Capacitor WebView는 로컬 파일을 직접 열기 때문에 실제 파일명이 필요하다.

작업 브랜치 `feat/session-catalog`의 엔진 관련 커밋 2개와 미커밋 변경은 이 배포에 포함되지 않았다.

### 마감 역산 일정 (공모전 마감 2026-09-21 16:00)

**D0 = 2026-08-17(월).** 사용자 확인(2026-08-21). 아래는 가정값이 아니라 실제 시작일 기준이다.

| 시점 | 할 일 | 비고 |
|---|---|---|
| 8/16~8/17 | privacy.html 배포, 앱 생성, 필수 설문·스토어 등록정보 입력, 비공개 트랙에 vc2 게시 | 완료 |
| 8/17(월) | **Doply 테스트 시작 = D0 = 1일째** | 완료 |
| 8/17 ~ **8/30(일)** | **12명 이상 14일 연속** opt-in 유지 (8/30이 14일째) | 이 14일은 단축 불가 |
| **9/1(화)** | 프로덕션 액세스 신청 (16일째 — 15일째 8/31보다 하루 여유를 둔다) | |
| 9/2 ~ 9/19 | 프로덕션 액세스 심사 → 프로덕션 출시 → 출시 검토 | **가장 큰 변수. 신규 개인 계정은 더 걸리기도 한다** |

당초 가정(8/20 시작)보다 **3일 앞당겨졌다.** 신청일 9/1 기준 마감까지 20일이 남으므로
심사 지연 여유가 그만큼 늘었지만, 여전히 마감 내 공개가 보장되지는 않는다.

**결론: 마감 내 프로덕션 공개는 가능하지만 보장되지 않는다.**
공모전은 이미 **웹 URL 단독으로 제출 준비가 끝나 있고**(`presentation/`), 규정상 웹은 스토어 등록이 불필요하다.
따라서 Play 등록은 실패해도 공모전에 손해가 없는 **병행 과제**로 진행한다.
9/7까지 프로덕션 공개가 확정되면 콘텐츠랩 제출을 `웹, 앱`으로 수정하고 기능설명서 2장도 함께 고친다.

## 1. 확정된 결정

- Google Play의 신규 개인 개발자 계정 비공개 테스트는 **Doply**를 사용한다.
- 서비스 URL: <https://www.doply.io/ko>
- 확인한 첫 이용 표시 가격: **USD 6.99**. 실제 청구 금액과 조건은 결제 화면에서 다시 확인한다.
- Doply에는 테스터 참여에 필요한 정보만 제공한다. Play Console 권한이나 서명 자격은 제공하지 않는다.
- Google의 기준은 최소 12명이 14일 연속 비공개 테스트에 opt-in하는 것이다. Doply의 표시 기간이 더 길더라도 Google Play Console 대시보드의 충족 상태를 최종 기준으로 삼는다.

## 2. 현재 상태

- [x] **R3 서명 AAB 생성 완료 (2026-08-25) — 지금 올릴 파일은 이것**
  - 파일: **`web/android/spindle-1.0-vc4-release.aab`**
  - versionCode: `4` / versionName: `1.0` (병합 매니페스트로 확인)
  - 크기: 8,604,142 bytes
  - `jarsigner -verify`: `jar verified.`
  - **서명키 지문이 vc2·vc3과 일치** — `SHA256:8B:45:7A:99:8D:AE:8E:FB:1B:C5:FB:98:AD:44:9D:BF:03:B2:7F:D1:CE:0E:0C:1F:27:43:83:62:52:32:50:1C`
    (키가 바뀌면 업로드가 거부되므로 매 릴리스 확인한다)
  - **권한이 vc3과 완전히 동일**하다 — 병합 매니페스트에 `INTERNET`·`ACCESS_COARSE_LOCATION`·
    `ACCESS_FINE_LOCATION` 셋뿐. `@capacitor/share`·`@capacitor/filesystem` 둘 다 자체
    `AndroidManifest.xml`이 비어 있어 권한을 추가하지 않는다(설치본 확인).
    **데이터 안전·권한 선언을 다시 손댈 일이 없다.**
  - 번들 내용 검증: `SharePlugin`·`FilesystemPlugin` 네이티브 클래스가
    `capacitor.plugins.json`에 등록됨, FileProvider `kr.spindle.app.fileprovider` 유지,
    번들된 웹 자산에 R3 문구 4종(공유 시트 안내·폴백 질문·폴백 링크·`m.map.kakao.com`) 포함
  - 이전 번들은 올리지 않는다: `spindle-1.0-vc3-release.aab`·`spindle-1.0-vc2-release.aab`(게시 완료),
    `superseded-do-not-upload_spindle-1.0-vc1.aab`(웹과 어긋남)
- [x] 릴리스 키스토어와 설정 파일이 Git에서 제외됨
  - `web/android/spindle-release.jks`
  - `web/android/keystore.properties`
- [x] AAB도 Git에서 제외됨
- [x] 비공개 테스트 서비스는 Doply로 결정
- [x] **실제 Android 휴대기기 인증 완료** (2026-08-16, 사용자 확인)
- [x] **Play 등록 자산 규격 검증·수정 완료** (2026-08-16)
  - 휴대전화 스크린샷 **5장**: 1080×1920 (9:16). 기존 1080×2400은 Play 규정
    "최대 변은 최소 변의 2배를 넘을 수 없다"(2400 > 2160) 위반이라 **업로드 거부 대상**이었다.
    `tools/play-screenshots.py`가 원본을 자르지 않고 1080×1920 캔버스에 맞춰 재생성한다.
  - 피처 그래픽 1024×500: RGBA → **RGB로 변환**(Play는 피처 그래픽에 알파 채널 금지)
  - 앱 아이콘 512×512 RGBA: 규격 그대로 유지(아이콘은 알파 필요)
  - 텍스트 메타데이터 길이 확인: 제목 7/30, 짧은 설명 33/80, 자세한 설명 372/4000, 변경사항 48/500
- [x] **`privacy.html` 프로덕션 배포 완료** (2026-08-17) — `https://spindle-6vp.pages.dev/privacy` 200 확인
- [x] **Spindle 앱 생성 완료** (2026-08-17, 사용자 확인)
- [ ] ⚠ `5_settings.png` 재캡처 — **선택**. 안 할 거면 스크린샷 1~4번만 올린다
      (2026-08-17 UI 변경으로 실제 화면과 다름. 사람만 할 수 있는 작업)
- [ ] 필수 앱 콘텐츠 설문 입력 (`docs/play-console-입력값.md` 2절)
- [ ] 스토어 등록정보 입력 (문안·아이콘·그래픽 이미지·스크린샷)
- [x] Doply 테스터 목록을 비공개 트랙에 등록
- [x] **서명된 AAB(vc2)를 비공개 트랙에 게시** (사용자 확인, 2026-08-21)
- [x] **Doply 테스트 시작** — 진행 중 (사용자 확인, 2026-08-21)
- [ ] **테스트 기간 릴리스 R2(vc3, D+4)·R3(vc4, D+8) 게시** — 3절 E-2 (R2 완료, R3 게시 대기)
      - [x] R2 코드·테스트·문서 완료 (2026-08-21) — 아래 "R2 구현 완료" 참고
      - [x] R2 웹 배포 완료 (2026-08-21, Actions `32438710860` 3잡 성공)
      - [x] **R2 AAB(vc3) 빌드·서명 검증 완료** (2026-08-21)
      - [x] **R2 AAB(vc3)를 비공개 테스트 트랙에 게시** (2026-08-21, 사용자 확인)
            — `비공개 테스트 – Alpha`, `3 (1.0)`, **전체 출시**. 검사·검토 통과 대기 상태로 넘겼다.
            권한 목록은 vc2와 동일함을 번들에서 확인했다(`@capacitor/app`은 권한을 추가하지 않는다)
            — 데이터 안전·권한 선언을 다시 손댈 일이 없다.
      - [x] R3 코드·테스트·문서 완료 (2026-08-25) — 아래 "R3 구현 완료" 참고
      - [x] **R3 AAB(vc4) 빌드·서명 검증 완료** (2026-08-25)
      - [ ] R3 웹 배포 (`main` 푸시 → GitHub Actions)
      - [ ] **R3 AAB(vc4)를 비공개 테스트 트랙에 게시** — 사람만 할 수 있는 작업
- [ ] 피드백 로그 작성 (3절 E-2) — 프로덕션 액세스 설문의 원천
- [ ] 12명 이상, 14일 연속 요건 충족
- [ ] 프로덕션 액세스 신청

### 현재 바로 해야 할 일 (2026-08-25 기준)

비공개 테스트 **9일째**다(D0 = 8/17). 지금 가장 중요한 것은 **14일 연속 카운터를 끊지 않는 것**이다
(3절 E의 금지 항목 — 트랙 일시중지·테스터 목록 교체·국가 제거·패키지명/서명키 변경 금지).
남은 카운터는 **8/30(일)** 에 14일째가 된다.

1. Doply 대시보드에서 매일 설치 유지 12대 이상을 확인한다.
2. **R3(vc4) 게시 — 오늘 할 일.** 코드·테스트·AAB 빌드·서명 검증은 끝났다
   (`web/android/spindle-1.0-vc4-release.aab`). 사람이 해야 하는 두 가지가 남았다:
   - `main` 푸시 → GitHub Actions로 웹 배포 (웹·앱 내용 일치 — 공모전 규정)
   - Play Console 비공개 트랙에 **새 출시 추가**로 vc4 업로드
     (트랙 일시중지·테스터 목록 변경 금지)
3. 지인 테스터 2~3명에게 실기기 확인을 부탁하고 피드백 로그에 적는다 (3절 E-2).
   R3는 **실기기에서만 확인 가능한 항목이 많다** — 아래 "R3 실기기 확인 목록" 참고.

배포본을 다시 검증해야 할 때:

```bash
curl -sL -o /dev/null -w "%{url_effective} %{http_code} %{size_download}\n" \
  https://spindle-6vp.pages.dev/privacy
# https://spindle-6vp.pages.dev/privacy 200 6275 가 나와야 한다
```

## 3. 등록 및 비공개 테스트 절차

### A. Play Console 개발자 계정과 앱 설정

1. 실제 Android 휴대폰으로 위 기기 인증을 완료한다.
2. Play Console 홈에 남은 개발자 계정 필수 작업을 모두 완료한다.
3. Spindle 앱을 만들거나 기존 앱을 선택한다.
4. 패키지명이 `kr.spindle.app`인지 확인한다.
5. 스토어 등록정보, 앱 액세스, 광고, 콘텐츠 등급, 타겟층, 개인정보처리방침, 데이터 안전 등 필수 항목을 완료한다.
6. 문안과 설문 판단 근거는 `docs/store-listing.md` 및 `fastlane/metadata/android/ko-KR/`를 사용한다.

업로드할 자산은 전부 규격 검증을 마쳤다(2절 참고).

**화면 순서대로 복사·붙여넣을 값은 `docs/play-console-입력값.md`에 정리해 뒀다.**
스토어 등록정보 문안, 앱 액세스·광고·콘텐츠 등급·타겟층 설문 답, 데이터 안전 답변표,
비공개 출시 노트, 프로덕션 액세스 신청서 항목까지 그 문서 하나로 끝난다.

### B. Doply 테스터 목록 등록

1. Doply 한국어 사이트에 로그인한다.
2. 대시보드에서 Spindle을 새 앱으로 등록한다.
3. Doply가 제공하는 테스터 이메일 CSV를 다운로드한다.
4. Play Console에서 `테스트 및 출시 > 테스트 > 비공개 테스트 > 트랙 관리 > 테스터`로 이동한다.
5. 이메일 목록 `doply-spindle`을 만들고 Doply CSV를 업로드한다.
6. 생성한 목록을 현재 비공개 트랙의 테스터로 선택한다.
7. 본인이 관리하는 피드백 이메일 또는 URL을 입력하고 저장한다.

CSV 업로드는 같은 목록의 기존 주소를 덮어쓸 수 있다. 지인 테스터는 별도 이메일 목록으로 관리하거나 업로드 전 CSV에 포함한다.

### C. 비공개 트랙에 AAB 게시

1. 비공개 테스트 트랙에서 새 출시를 만든다.
2. `web/android/spindle-1.0-vc2-release.aab`를 직접 Play Console에 업로드한다.
   2026-08-17에 `main` `c1d15bc` 기준으로 재빌드한 versionCode 2 번들이며, 서명·패키지명·
   번들된 웹 자산 내용까지 검증했다. 8/14 빌드(vc1)는 웹 배포본과 어긋나므로 올리지 않는다.

   다시 빌드해야 할 때 (web 코드가 바뀐 뒤):

   ```bash
   cd web && npm run build:app && npx cap sync android
   cd android && ./gradlew bundleRelease
   # 산출물: web/android/app/build/outputs/bundle/release/app-release.aab
   ```

   `web/.env.local`(카카오 JS 키)과 `web/android/keystore.properties`가 있어야 하며 둘 다
   git에서 제외돼 있다. 코드가 바뀌면 `versionCode`를 반드시 올린다.
3. 출시 이름 예시: `Spindle 1.0 비공개 테스트`
4. 출시 노트 예시:

   > Spindle 1.0 첫 비공개 테스트 버전입니다. 위치 기반 부산 관광 스핀 기능과 관광지 탐색 기능을 테스트합니다.

5. 오류와 경고를 검토한 뒤 비공개 테스트로 출시하거나 검토를 위해 변경사항을 전송한다.
6. 상태가 `초안` 또는 `검토 중`이 아니라 `게시됨`, `사용 가능` 또는 이에 준하는 활성 상태가 될 때까지 기다린다.
7. 첫 테스트 링크 활성화에는 몇 시간이 걸릴 수 있다.

### D. Doply에서 설치 가능 여부 확인 후 결제

1. Play Console 비공개 트랙의 `테스터` 탭에서 Android 참여 링크를 복사한다.
2. 링크는 보통 `https://play.google.com/apps/testing/kr.spindle.app` 형태다. 화면에 표시된 실제 링크를 사용한다.
3. Doply의 Spindle 설정에 참여 링크를 입력한다.
4. Doply의 설치 가능 여부 검사를 실행한다.
5. 최소 12대 이상이 설치 가능한 상태인지 확인한다.
6. 설치 가능 기기가 부족하면 아래 항목을 확인한다.
   - `doply-spindle` 목록이 비공개 트랙에 선택되어 있는가
   - 변경사항을 저장했는가
   - 비공개 출시가 게시 완료됐는가
   - 국가/지역 제한이 Doply 기기를 막고 있지 않은가
   - 내부 테스트 링크가 아니라 비공개 테스트 참여 링크인가
7. 설치 가능 상태를 확인한 뒤에만 첫 이용가 USD 6.99 결제를 진행한다.
8. Doply에서 테스트 시작을 누른다.

### E. 테스트 기간 운영

1. Doply 대시보드에서 첫날 설치 기기가 12대 이상인지 확인한다.
2. 매일 설치 유지, 실행 기록, 스크린샷, 업데이트 상태와 중도 이탈을 확인한다.
3. 테스트 중 다음 항목을 변경하거나 중지하지 않는다.
   - 비공개 트랙 일시중지
   - `doply-spindle` 목록 삭제 또는 해제
   - 참여 국가 제거
   - 패키지명 변경
   - 서명키 변경
4. 실제 지인 2~3명 이상에게 위치 권한, 스핀, 센서 방향, POI 선택을 실기기에서 테스트하도록 요청한다.
5. Play Console 비공개 의견 또는 별도 기록으로 실제 피드백을 남긴다.
6. 피드백을 반영해 업데이트할 경우 versionCode를 `2`, `3`처럼 증가시키고 같은 릴리스 키로 서명한다.
7. 단순히 업데이트 횟수를 채우기 위해 내용이 동일한 번들을 반복 제출하지 않는다.

### E-2. 테스트 기간 릴리스 계획 (3~4일 간격)

#### 왜 하는가 — 구글 요건과 Doply 권장을 구분한다

| 출처 | 내용 | 성격 |
|---|---|---|
| **구글 공식** | 테스터 **12명 이상**이 **14일 연속** opt-in, 신청 시점에도 12명 유지 | **필수 요건** |
| **구글 공식** | 프로덕션 액세스 신청서를 사람이 검토 — 테스터 모집 방법·받은 피드백·**반영한 변경사항**·준비 판단 근거를 묻는다 | **필수 절차** |
| Doply 권장 | 14일 내 3~4일 간격으로 **최소 2회 이상** 신규 버전 출시 | 구글 규정 **아님**. Doply 노하우 |
| Doply 약관 | 2회 이상 업데이트 이력이 있는데 거절되면 **18일 무료 연장** | 구글 무관. Doply 자체 보상 |

즉 버전 출시 횟수·간격은 구글 규정이 아니다. 그래도 따를 이유가 두 가지 있다.

1. **설문에 쓸 재료가 생긴다.** "피드백을 반영해 무엇을 바꿨는가"에 답해야 하는데 버전이 하나면 쓸 말이 없다. 요건을 채웠는데도 거절되는 사례의 대부분이 "실제로 테스트한 흔적이 없어 보인다"다.
2. **Doply 연장 안전망의 조건이다.**

> ⚠ **업데이트 횟수보다 14일 연속 카운터가 훨씬 중요하다.** 릴리스를 만지다 트랙을 일시중지하거나 테스터 목록을 교체하면 카운터가 끊기고 처음부터 다시다 (3절 E의 금지 항목). 업데이트는 **새 출시를 추가**하는 것이며 트랙을 건드리는 일이 아니다.

#### 고르는 기준

- **웹 코드에서 같이 빌드되는 개선을 우선한다.** 같은 `web/` 코드가 Cloudflare Pages와 AAB 양쪽으로 나가므로, 하나의 작업이 Play 테스트 이력과 공모전 완성도(30점) 배점에 동시에 얹힌다.
- **앱 전용 작업은 꼭 필요한 것만.** 아래 계획에서 앱 전용은 뒤로가기·공유 시트 두 건뿐이다.
- **절대 원칙을 깨는 개선은 후보에서 제외한다** (AGENTS.md 1~6). 특히 재시도·오프라인 관련 작업이 TourAPI 응답을 영속 저장하게 되면 안 된다(원칙 3).
- 내용이 같은 번들을 횟수 채우기로 반복 제출하지 않는다.

#### 일정 (D0 = 2026-08-17(월), 사용자 확인)

| 릴리스 | 시점 | versionCode | 주제 | 상태 |
|---|---|---:|---|---|
| R1 | D0 · **8/17(월)** | 2 | 베이스라인 | 게시 완료 |
| **R2** | D+4 · **8/21(금)** | **3** | 첫 실행에서 막히는 것 | 게시 완료 |
| **R3** | D+8 · **8/25(화)** | **4** | 핵심 기능 품질 | 코드·AAB 완료, 웹 배포·게시 대기 |
| R4 | D+12 · **8/29(토)** | 5 | 실측 기반 안정화 (여유분 — R2·R3만으로 조건 충족) | |

⚠ **R4는 토요일이다.** Doply 테스터의 업데이트 수행이 주말에 늦어질 수 있으니, R4를 하려면
8/28(금)로 당기는 편이 안전하다. R2·R3만으로 Doply 조건(2회 이상)은 이미 충족된다.

14일 연속 카운터는 **8/30(일)** 에 14일째가 된다. R3(8/25)·R4(8/28~29)는 모두 그 안쪽이므로
**출시 추가가 카운터를 건드리지 않도록** 3절 E의 금지 항목(트랙 일시중지·테스터 목록 변경)을
특히 조심한다.

게시 타이밍: Doply 테스터의 업데이트 수행 시각이 정해져 있다. **그 시각 전에 게시**해야 당일 반영된다. Doply 대시보드에서 시각을 확인하고 반나절 여유를 둔다. 비공개 트랙 출시도 검토를 거치므로 3~4일 간격이면 충분하다.

---

#### R2 (vc3) — 첫 실행에서 막히는 것

| # | 개선 | 범위 | 근거 |
|---:|---|---|---|
| 1 | **안드로이드 하드웨어 뒤로가기 처리** — 화면 스택을 거슬러 이동하고, 홈에서 한 번 더 누르면 `뒤로 한 번 더 누르면 종료` 안내 후 종료 | 앱 전용 (`@capacitor/app`) | **현재 미처리.** 결과 카드에서 뒤로가기를 누르면 앱이 즉시 종료된다. 테스터가 100% 부딪히는 문제 |
| 2 | **네트워크 실패 복구** — TourAPI 호출 실패 시 원인이 읽히는 문구 + `다시 시도` 버튼 | 웹+앱 | 실기기는 이동 중 네트워크가 끊긴다. 공모전 완성도(안정성)에도 직결 |

- 1번은 `@capacitor/app` 신규 설치 → `npx cap sync android` 필요.
- 2번은 **재시도 결과를 영속 저장하지 않는다** (원칙 3). 세션 메모리 재사용까지만.

릴리스 노트 (한국어, 500자 제한):

> 안드로이드 뒤로가기 버튼으로 이전 화면으로 돌아갈 수 있도록 수정했습니다. 홈 화면에서는 한 번 더 누를 때 종료됩니다. 이동 중 네트워크가 불안정할 때 관광 정보를 불러오지 못하면 사유를 안내하고 다시 시도할 수 있게 했습니다.

(같은 문안이 `fastlane/metadata/android/ko-KR/changelogs/3.txt`에 있다.)

##### R2 구현 완료 (2026-08-21) — 남은 것은 빌드·게시뿐

`npm run check` 통과(guard + typecheck + lint + 292 테스트 + proxy). `versionCode 3` 반영 완료.

| 항목 | 구현 |
|---|---|
| 뒤로가기 목적지 | `web/src/navigation/backAction.ts` — 14개 화면 전부에 대해 **그 화면의 ← 버튼과 같은 목적지**를 돌려주는 순수 함수. 탭 화면은 홈으로, 리빌은 스핀으로 (둘 다 기존에 back 경로가 없어 앱이 죽던 구간) |
| 오버레이 우선 닫기 | `web/src/navigation/backGuards.ts` — LIFO 가드 스택. 갤러리·[자세히 보기] 시트·핀 시트·코스 안내 단계·코치마크가 각자 등록한다. 화면 상태를 App으로 끌어올리지 않았다 |
| 두 번 눌러 종료 | `web/src/navigation/exitIntent.ts` (순수 판단) + `.exit-notice` 한 줄 안내. 2초 창 |
| 네이티브 연결 | `web/src/navigation/useHardwareBack.ts` — `Capacitor.isNativePlatform()` 뒤에서 `@capacitor/app`을 **동적 import**. 웹 번들·웹 동작은 그대로 |
| 실패 원인 | `TourApiError.kind` (`offline`·`timeout`·`network`·`http`·`api`) + `web/src/api/failureCopy.ts`의 공통 문구 |
| 적용 화면 | 혼잡 예측·방문 정보·축제 — 기존 시각 처리를 그대로 두고 사유 절만 덧붙였다 |
| 신설 | 명소 지도 '관광공사 등록 명소' 실패에 사유 + [다시 시도] (기존에는 `console.warn`뿐이고 재시도 수단이 없었다) |

`npx cap update android`로 네이티브 프로젝트에 플러그인을 물려 뒀다 —
`web/android/capacitor.settings.gradle`의 `:capacitor-app` include와
`web/android/app/capacitor.build.gradle`의 dependency가 커밋에 포함돼 있다.
**이 두 파일이 빠지면 빌드는 되지만 뒤로가기가 동작하지 않는다** (네이티브 구현을 못 찾는다).

확인해 둔 것: `@capacitor/app` 8.x의 `AppPlugin.handleOnBackPressed()`는 **JS 리스너가 있으면
`backButton` 이벤트만 던지고 `webView.goBack()`도 액티비티 종료도 하지 않는다**(설치본 소스 확인).
리스너가 없을 때 액티비티가 그대로 finish 되는 것이 지금 앱이 죽는 원인이다. 종료는 `App.exitApp()`으로
명시 호출한다. 앱은 `history.pushState`를 쓰지 않으므로 뒤로가기가 이중 처리되지 않는다.

###### 실기기 확인 목록 (사람만 가능)

- [ ] 결과 카드 → 뒤로 → 진입 화면으로 복귀 (**앱이 죽지 않는다** — 이번 릴리스의 본론)
- [ ] 결과 카드에서 갤러리·[자세히 보기]를 열고 뒤로 → 오버레이만 닫히고 화면은 유지
- [ ] 명소 지도 핀 시트 열고 뒤로 → 시트만 닫힘
- [ ] 코스 `안내 시작` 후 뒤로 → 안내만 종료, 한 번 더 → 복귀
- [ ] 홈 코치마크 열고 뒤로 → 코치마크만 닫힘
- [ ] 출발점 → 지도에서 찍기 → 뒤로 → 출발점 → 뒤로 → 원래 화면
- [ ] 리빌 연출 중 뒤로 → 스핀 (3초 뒤 결과가 뒤늦게 뜨지 않는다)
- [ ] 홈에서 뒤로 1회 → 안내, 2초 내 1회 더 → 종료 / 3초 뒤 → 안내부터 다시
- [ ] 명소·스핀·도장·설정 탭에서 뒤로 → 홈 (앱 종료 아님)
- [ ] 비행기 모드로 축제·명소·결과 진입 → 원인 문구 + [다시 시도], 해제 후 재시도하면 정상 로드
- [ ] 앱을 껐다 켜면 이전 실패·응답이 남아 있지 않다 (영속 저장 없음)

#### R3 (vc4) — 핵심 기능 품질

| # | 개선 | 범위 | 근거 |
|---:|---|---|---|
| 3 | **네이티브 공유 시트** — 공유 카드 PNG를 안드로이드 공유 시트로 전달 | 앱 전용 (`@capacitor/share` + `@capacitor/filesystem`) | **현재 미설치.** WebView의 Web Share는 파일 공유가 제한적이라 핵심 기능이 앱에서 반쪽이다. 웹은 기존 경로 유지 |
| 4 | **카카오맵 딥링크 폴백** — 앱 미설치·팝업 차단 시 모바일 웹 길찾기로 이어짐 | 웹+앱 | 기능설명서의 미검증 항목과 동일. 코스 안내가 끊기는 유일한 지점 |

릴리스 노트 (한국어, 500자 제한 — 140자):

> 공유 카드를 안드로이드 공유 시트로 바로 보낼 수 있게 했습니다. 메신저·사진 앱 등 원하는 앱을 골라 카드 이미지를 그대로 보낼 수 있습니다. 카카오맵이 설치되지 않았거나 링크가 열리지 않을 때는 모바일 웹 지도로 이어서 길찾기를 계속할 수 있게 했습니다.

(같은 문안이 `fastlane/metadata/android/ko-KR/changelogs/4.txt`에 있다.)

##### R3 구현 완료 (2026-08-25) — 남은 것은 웹 배포·게시뿐

`npm run check` 통과(guard 164파일 + typecheck + lint + web 311 테스트 + proxy 20 테스트).
`versionCode 4` 반영 완료. **Codex 교차 검수 반영 완료**(아래 별도 절). 실기기 확인 목록은 뒤에 있다.

###### 3번 — 네이티브 공유 시트

**앱에서는 공유 카드를 밖으로 내보낼 방법이 아예 없었다.** 둘 다 죽어 있었다:

- 안드로이드 WebView는 Web Share API를 구현하지 않아 `navigator.share`가 **존재하지 않는다**
  → 기존 `canShare` 판정이 false라 `[공유하기]` 버튼 자체가 앱에서 렌더되지 않았다.
- Capacitor는 `setDownloadListener`를 걸지 않는다(설치본 소스 확인) → `[이미지로 저장]`의
  `<a download>` blob 저장이 **조용히 무시된다**. 눌러도 아무 일도 일어나지 않는 버튼이었다.

| 항목 | 구현 |
|---|---|
| 전달 경로 | `web/src/lib/shareCardDelivery.ts` — PNG를 `Directory.Cache`에 파일로 쓴 뒤 `Share.share({ files: [uri] })`로 안드로이드 공유 시트에 넘긴다 |
| FileProvider | 추가 설정 없이 동작한다. `Share` 플러그인이 `${applicationId}.fileprovider`로 감싸고, 기존 `res/xml/file_paths.xml`의 `cache-path`가 `Directory.Cache`(=`context.cacheDir`)를 이미 덮고 있다 |
| 캐시 누적 방지 | **파일명을 고정**(`spindle-share-card.png`)해 캐시에 존재할 수 있는 사본을 항상 최대 한 개로 묶는다. 매 공유 **시작 시** 그 하나를 지우고 새로 쓴다 — 앱을 껐다 켜도 직전 카드 한 장만 남고 그마저 다음 공유에서 사라진다. 공유 직후에 지우면 수신 앱이 스트림을 읽기 전에 파일이 없어져 경합이 나므로 "쓰기 직전 정리"로 잡았다 |
| 동시 실행 | 모듈 수준 `inFlight` Promise로 쓰기→공유→정리 구간을 직렬화한다. 연타 시 두 번째 호출은 진행 중인 공유 결과를 그대로 받는다 — 파일을 다시 쓰거나 지우지 않는다 |
| 취소 vs 실패 | `isShareDismissal()` — 사용자가 시트를 닫은 것(`Share canceled`)에 오류 문구를 띄우지 않는다. 넓은 정규식 대신 **실제 취소 메시지 allowlist + `AbortError`**만 허용한다(파일 I/O가 "aborted"된 진짜 실패를 삼키지 않도록) |
| 저장 버튼 | **앱에서는 `[이미지로 저장]`을 없앴다.** 위 이유로 동작하지 않는 버튼이기 때문이다. 대신 `공유할 앱을 고르면 카드 이미지가 그대로 전달돼요` 한 줄로 대체했다. **문구로 저장을 약속하지 않는다** — 안드로이드 공유 시트는 OS의 저장 기능이 아니라 `ACTION_SEND`를 받겠다고 선언한 설치 앱 목록일 뿐이라 사진·파일 앱이 뜬다는 보장이 없다. **웹은 기존 두 버튼 그대로다** |
| 출처 표기 | 공유 화면에 `출처: ⓒ한국관광공사`를 추가했다. 생성되는 PNG에는 원래 들어가 있었지만 **화면에는 없었다** — 이 화면도 TourAPI 관광지명·대표 이미지를 보여 주므로 절대 원칙 6 대상이다 (R3 이전부터 있던 누락) |
| **안드로이드에서만 켠다** | `canUseNativeShareSheet()`는 `nativePlatform() === 'android'`일 때만 true다. **iOS는 기존 경로를 그대로 쓴다** — WKWebView가 Web Share를 파일까지 구현하고, 무엇보다 `@capacitor/share`가 iOS 프로젝트에 물려 있지 않다(`ios/App/CapApp-SPM/Package.swift`에 `CapacitorApp`뿐). 셸 유무로만 판정하면 iOS에서 플러그인 프록시가 "not implemented"로 거절해 **공유가 통째로 죽는다** |
| 웹 영향 | 없다. 기존 `navigator.share` 경로를 그대로 탄다 |

###### 4번 — 카카오맵 길찾기 폴백

`map.kakao.com/link/*`는 모바일에서 `applink.map.kakao.com`으로 넘어가 앱을 먼저 띄우려 한다.
앱이 없으면 설치 안내에서 멈추고, 브라우저가 새 탭을 막으면 화면에 아무 변화가 없다.

| 항목 | 구현 |
|---|---|
| 폴백 주소 | `kakaoMapMobileSearchUrl()` — `https://m.map.kakao.com/actions/searchView?q=부산 <장소명>`. 모바일 웹 호스트를 직접 가리켜 앱 없이도 장소 화면까지 도달한다 |
| ⚠ 주소를 바꾸지 말 것 | 더 "공식"으로 보이는 `m.map.kakao.com/scheme/search`는 **오히려 `applink.map.kakao.com`(앱 설치 유도 페이지)으로 리디렉션된다** — 이 폴백이 벗어나려던 바로 그 화면이다. 모바일 UA로 확인(2026-08-25): `/scheme/search` → applink, 10,565B, 장소명 없음, 본문에 "설치" / `/actions/searchView` → 리디렉션 없음, 83,348B, 장소명 59회, 스토어 유도 없음. 근거는 `mapLinks.ts` 주석에도 남겼다 |
| **타 지도 서비스 사용 안 함** | 폴백도 **카카오 안에서 끝낸다.** 네이버지도 등 다른 지도로 넘기지 않는다 — 배점에 지도 제공사 항목이 없어 얻는 것이 없고, 경쟁사 브랜드 노출 리스크만 남기 때문 |
| 표시 방식 | `web/src/components/MapFallbackNote.tsx` — **카카오맵 링크를 실제로 누른 뒤에만** 조용한 한 줄(`카카오맵이 열리지 않았나요? 모바일 웹으로 열기`)이 붙는다. 누르기 전에는 보이지 않아 평소 화면은 그대로다 |
| 실패를 감지하지 않는 이유 | 감지할 방법이 없다. Capacitor WebView는 `window.open`이 **항상 null**을 돌려주면서도 실제로는 인텐트로 잘 열린다 — 반환값으로 판정하면 정상 동작에도 오탐이 난다 |
| 적용 위치 | 결과 카드 `[길찾기]`, 코스 장소별 `[카카오맵 길찾기]`, 코스 1단계 `[카카오맵으로 길찾기]`, 코스 2단계 `[전체 코스 보기]`(폴백은 1번 장소 기준), **축제 목록 카드**. 축제 링크 두 곳은 URL을 하드코딩하고 있었는데 `kakaoMapSearchUrl()`로 통일했다 |
| 표시 키 | 안내 단계 CTA는 POI 카드와 **다른 키**(`FIRST_STOP_LINK_ID`·`COURSE_LINK_ID`)를 쓴다. 같은 키면 1단계에서 누른 뒤 안내를 닫았을 때 누른 적 없는 스트립 카드에도 한 줄이 돋아난다 |
| 레이아웃 | 결과 카드 하단 바는 absolute라 한 줄이 붙으면 높아진다. `ACTION_BAR_CLEARANCE`로 스크롤 하단 여백과 도장 토스트 위치를 함께 올려 콘텐츠·토스트가 가려지지 않게 했다 |
| 좌표 | 폴백 주소에는 **장소명만** 담는다. 사용자 GPS·방위각은 넣지 않는다 (절대 원칙 1) |

⚠ **문서 정정 동반**: `docs/submission/기능설명서.md`·`AGENTS.md`·`SPEC.md`·`docs/ui.md`가
"길찾기는 카카오맵·**네이버지도** 딥링크"라고 적어 뒀는데 네이버는 구현된 적이 없었다.
이번 방침(카카오 전용)에 맞춰 네 문서를 모두 고쳤다.

###### 공통 — 웹 번들 오염 방지

`useHardwareBack`과 같은 규약을 적용했다. 네이티브 판정(`web/src/native/shell.ts`, R2의
`useHardwareBack`에서 추출) 뒤에서만 플러그인을 동적 import 하고, 웹 빌드에서는
`vite.config.ts`가 `@capacitor/share`·`@capacitor/filesystem`을 스텁으로 바꿔 끼운다.

빌드로 확인했다 — 웹 산출물에는 실물 대신 스텁 청크만 남는다(각 134~197 bytes):
`capacitorAppStub`·`capacitorShareStub`·`capacitorFilesystemStub`.

###### Codex 교차 검수 (2026-08-25) — 처리 결과

구현 후 `codex exec`로 읽기 전용 교차 검수를 돌렸다. 8건 지적 중 **6건 반영, 1건 반증, 1건 보류.**

| 심각도 | 지적 | 처리 |
|---|---|---|
| 높음 | 공유 카드 캐시 파일이 앱 재시작 후 삭제되지 않아 사본이 누적된다 (절대 원칙 3 해석 위험) | **반영** — 파일명 고정으로 사본 상한을 1개로 묶고 쓰기 직전에 정리 |
| 중간 | 전달 함수에 동시 실행 잠금이 없어 연타 시 서로의 파일을 지울 수 있다 | **반영** — 모듈 수준 `inFlight` 직렬화 |
| 중간 | `m.map.kakao.com/actions/searchView`는 비공식 엔드포인트다. 공식 `/scheme/search`로 바꿔라 | **반증 — 바꾸지 않았다.** 실제로 확인하니 `/scheme/search`가 `applink.map.kakao.com`(앱 설치 유도)으로 리디렉션된다. 폴백이 탈출하려던 화면이라 제안대로 바꾸면 기능이 무력화된다. 근거를 코드 주석과 위 표에 남겼다 |
| 중간 | 폴백 한 줄이 붙으면 하단 바가 커져 콘텐츠·도장 토스트와 겹친다 | **반영** — `ACTION_BAR_CLEARANCE`로 스크롤 여백·토스트 위치를 함께 조정 |
| 중간 | "사진 앱이나 파일로 저장할 수 있어요"는 보장할 수 없다 (Sharesheet는 설치 앱 목록일 뿐) | **반영** — "공유할 앱을 고르면 카드 이미지가 그대로 전달돼요"로 완화 |
| 중간 | 공유 화면 미리보기에 출처 표기가 없다 (절대 원칙 6, R3 이전부터) | **반영** — `SourceLine` 추가 |
| 낮음 | `/cancel\|dismiss\|abort/i`가 너무 넓어 진짜 실패를 취소로 삼킨다 | **반영** — 취소 메시지 allowlist + `AbortError` |
| 낮음 | 테스트가 네이티브 성공 경로를 전혀 실행하지 않는다 | **반영** — 플러그인을 `vi.mock`으로 가로채 호출 순서·정리·취소/실패·연타까지 14케이스로 검증 |

Codex가 "문제 없음"으로 확인한 것: 절대 원칙 1·2·4·5, base64 변환, `handleShare`의 `finally`
실행, `mapTried`/`triedMapIds` 초기화, 웹 런타임 회귀 없음.

**보류 1건**: Codex가 "추후 `vite dev`로 Capacitor live reload를 붙이면 스텁이 걸린다"는 조건부
주의를 달았다. 현재 앱 빌드는 `--mode app`이라 해당 없음 — live reload를 도입할 때 다시 본다.

###### PR #6 리뷰 (2026-08-25) — 처리 결과

Codex 검수와 별개로 PR 단계 코드 리뷰를 한 번 더 돌렸다. 7건 중 **6건 반영, 1건은 PR 밖.**

| 심각도 | 지적 | 처리 |
|---|---|---|
| 높음 | `changelogs/3.txt`에 무관한 프롬프트 문서 1,016자가 붙어 있다 (Play 500자 제한 초과) | **PR 밖 — 커밋하지 않은 다른 작업의 변경이라 손대지 않았다.** vc3 릴리스 노트라 Play에 올라간 원본과 어긋난 상태이므로 사람이 되돌려야 한다 |
| 중간 | **iOS 앱에서 공유가 통째로 죽는다.** `@capacitor/share`가 iOS 프로젝트에 없는데 `isNativeShell()`만 보고 네이티브 경로로 보내고, `[이미지로 저장]`은 숨겨진다. R3 이전에는 `navigator.share`로 잘 되던 기능이다 | **반영** — `nativePlatform()`을 추가해 **안드로이드에서만** 네이티브 시트를 켠다. iOS는 기존 경로 유지 |
| 낮음 | 폴백 한 줄이 액션 바 그라디언트의 투명 구간에 놓여 스크롤 본문 위에 겹쳐 읽힌다 | **반영** — 한 줄에 자체 배경을 깔았다 |
| 낮음 | 코스 1단계에서 길찾기를 누르면 누른 적 없는 스트립 카드에도 폴백이 돋아난다 | **반영** — `FIRST_STOP_LINK_ID`로 키를 분리 |
| 낮음 | `if (inFlight) return inFlight`가 **다른 카드**를 공유했을 때 앞 카드의 결과를 돌려준다 (시트를 열어 둔 채 다른 POI로 이동하는 경우) | **반영** — 진행 중 Promise를 공유하지 않고 **뒤에 이어서 실행**하도록 바꿨다 |
| 낮음 | 실기기 확인 목록의 문구가 완화 전 옛 문구라 테스터가 실패로 표시한다 | **반영** — 현재 문구로 갱신 |
| 낮음 | 축제 링크 두 곳이 URL을 하드코딩하고 폴백도 없어 같은 applink 벽에 부딪힌다 | **반영** — `kakaoMapSearchUrl()`로 통일하고 축제 목록 카드에 폴백 한 줄 추가 |

반영 후 `npm run check` 재통과 — guard 164파일 · web **314** 테스트 · proxy 20 테스트.

###### R3 실기기 확인 목록 (사람만 가능)

- [ ] 결과 카드 → 공유 → `[공유하기]` → **안드로이드 공유 시트가 뜬다**(이번 릴리스의 본론)
- [ ] 시트에서 메신저로 보내면 **카드 PNG가 이미지로 도착한다**(링크나 빈 파일이 아니다)
- [ ] 시트에서 사진 앱/파일로 저장 → 갤러리·파일에 1080×1920 PNG가 남는다
- [ ] 시트를 그냥 닫으면 **오류 문구가 뜨지 않는다**
- [ ] 공유를 여러 번 반복해도 캐시에 카드가 쌓이지 않는다(직전 파일을 지운다)
- [ ] 앱 화면에 `[이미지로 저장]` 버튼이 없고 `공유할 앱을 고르면 카드 이미지가 그대로 전달돼요` 한 줄이 보인다
- [ ] **웹(모바일 브라우저)** 공유 화면은 예전 그대로다 — `[공유하기]`·`[이미지로 저장]` 두 버튼
- [ ] 결과 카드 `[길찾기]` 누름 → 카카오맵이 열리고, 화면에 `카카오맵이 열리지 않았나요?` 한 줄이 생긴다
- [ ] 그 줄의 `모바일 웹으로 열기` → **카카오맵 앱을 지운 기기에서도** 장소 화면까지 도달한다
- [ ] 코스 1단계·2단계·장소별 길찾기에서도 같은 한 줄이 누른 뒤에만 나타난다
- [ ] 다른 후보로 넘기면 폴백 한 줄이 사라진다(다시 누르기 전까지)
- [ ] 축제 목록 카드를 누른 뒤에도 같은 폴백 한 줄이 그 카드에만 나타난다
- [ ] 코스 1단계에서 길찾기를 누르고 안내를 닫아도, 아래 스트립 1번 카드에는 한 줄이 생기지 않는다
- [ ] 뒤로가기(R2 기능)가 여전히 정상 — 공유 화면에서 뒤로 → 결과 카드

#### R4 (vc5) — 실측 기반 안정화 (여유분)

| # | 개선 | 범위 | 근거 |
|---:|---|---|---|
| 5 | **Play Console 앱 요약(vitals)에 잡힌 크래시·ANR 수정** | 웹+앱 | 추측이 아닌 실측 데이터. 설문에 가장 강한 재료 |
| 6 | **나침반 정확도 낮을 때 보정 안내** (8자 흔들기 안내) | 웹+앱 | 실기기 나침반은 자기 간섭에 약하다. 지인 테스터 피드백 소재 |
| 7 | 저사양 기기에서 명소 지도 핀 렌더링 성능 | 웹+앱 | 핀이 많을 때 체감 |

R2·R3만으로 Doply 조건(2회 이상)은 충족된다. R4는 vitals에 실제로 잡힌 것이 있을 때만 진행하고, 없으면 6·7번으로 채우거나 생략한다.

---

#### 피드백 로그 (프로덕션 액세스 설문의 원천)

신청서에 그대로 옮길 수 있도록 **날짜·출처·내용·조치**를 남긴다. 기억으로 쓰면 설문이 빈약해진다.

| 날짜 | 출처 | 피드백 | 조치 | 반영 릴리스 |
|---|---|---|---|---|
| 2026-08-21 | 자체 실기기 확인 | 결과 카드에서 안드로이드 뒤로가기를 누르면 이전 화면으로 가지 않고 앱이 곧바로 종료된다 | 화면 스택을 거슬러 이동하도록 처리하고, 홈에서만 두 번 눌러 종료하게 바꿨다 | vc3 |
| 2026-08-21 | 자체 실기기 확인 | 이동 중 연결이 끊기면 "잠시 연결이 어려워요"만 떠서 무엇이 문제인지 알 수 없다 | 오프라인·응답 지연·서버 오류를 구분해 사유를 안내하고, 재시도 수단이 없던 명소 지도에도 [다시 시도]를 추가했다 | vc3 |
| 2026-08-25 | 자체 실기기 확인 | 앱에서 공유 카드를 만들면 `[이미지로 저장]`을 눌러도 아무 일도 일어나지 않고, `[공유하기]` 버튼은 아예 보이지 않는다 — 카드를 밖으로 꺼낼 방법이 없다 | 안드로이드 공유 시트로 PNG를 넘기도록 바꾸고, 동작하지 않던 저장 버튼은 시트 안내 문구로 대체했다 | vc4 |
| 2026-08-25 | 자체 실기기 확인 | 카카오맵을 설치하지 않은 기기에서 `[길찾기]`를 누르면 설치 안내에서 멈춰 코스 진행이 끊긴다 | 링크를 누른 뒤 카카오 모바일 웹으로 이어갈 수 있는 한 줄을 결과 카드·코스 전 구간에 추가했다 | vc4 |
| | Doply 테스터 / 지인 / Play Console 비공개 의견 | | | vc5 |

지인 테스터 2~3명에게는 **실기기에서 위치 권한·스핀·센서 방향·POI 선택**을 직접 시켜보고 말로 받은 피드백까지 적는다.

#### 릴리스마다 지킬 것

- [x] R2: `versionCode` 3으로 증가 (`web/android/app/build.gradle`) — 2026-08-21
- [x] R3: `versionCode` 4로 증가 (`web/android/app/build.gradle`) — 2026-08-25
- [ ] `versionCode`를 반드시 증가 (`web/android/app/build.gradle`)
- [ ] **같은 릴리스 키(`spindle-release.jks`)로 서명** — 키 변경 시 업로드 거부
- [ ] 패키지명 `kr.spindle.app` 유지
- [ ] `npm run check` 통과 후 빌드 (guard + typecheck + lint + test)
- [ ] `jarsigner -verify`로 서명 확인
- [ ] 비공개 트랙에 **새 출시 추가** — 트랙 일시중지·테스터 목록 변경 금지
- [ ] 웹에도 반영되는 개선이면 `main` 푸시로 Cloudflare 배포까지 함께 (웹·앱 내용 일치 — 공모전 규정 "두 종류 이상 개발 시 동일 내용")
- [ ] 피드백 로그에 이 릴리스가 무엇을 고쳤는지 기록

빌드 명령:

```bash
cd web && npm run build:app && npx cap sync android
cd android && ./gradlew bundleRelease
# 산출물: web/android/app/build/outputs/bundle/release/app-release.aab
```

### F. 프로덕션 액세스 신청

1. 최소 12명이 14일 연속 opt-in한 상태인지 Play Console 대시보드에서 확인한다.
2. 정확히 14일이 되는 순간보다 15~16일째 확인 후 신청하는 편이 안전하다.
3. `대시보드 > 프로덕션 액세스 신청`으로 이동한다.
4. 다음 내용을 실제 테스트 기록에 맞춰 작성한다.
   - 테스터 모집 방식
   - 사용한 주요 기능
   - 받은 피드백
   - 피드백을 반영한 변경사항
   - 프로덕션 준비가 됐다고 판단한 근거
5. 승인 결과가 나올 때까지 비공개 트랙과 테스터 목록을 유지한다.

## 4. 보안 금지사항

Doply 또는 다른 제3자에게 아래 파일과 권한을 절대 제공하지 않는다.

- `web/android/spindle-release.jks`
- `web/android/keystore.properties`
- 키스토어 비밀번호 또는 키 별칭 비밀번호
- Play Console 로그인 비밀번호
- Play Console 사용자 또는 관리자 권한
- Google Play Developer API 서비스 계정 JSON

AAB 업로드는 프로젝트 소유자가 Play Console에서 직접 수행한다. Doply에는 테스터 CSV 등록에 필요한 흐름과 비공개 테스트 참여 링크만 사용한다.

릴리스 키스토어와 `keystore.properties`는 OneDrive 외의 별도 저장소에도 백업한다. OneDrive 동기화는 삭제도 동기화되므로 단독 백업으로 보지 않는다.

## 5. 다음 세션 시작 체크포인트

기기 인증과 자산 준비는 끝났다. 다음 세션은 사용자에게 아래를 먼저 확인한다.

1. Spindle 앱 생성과 필수 앱 콘텐츠 설정이 어디까지 완료됐는가 (`docs/play-console-입력값.md` 기준)
2. 비공개 트랙에 AAB를 게시했는가 / Doply 테스트를 시작했다면 며칠째인가
3. (회귀 확인) `curl -sL .../privacy` 가 여전히 정책 본문 200을 주는가

완료 상태에 따라 2절 체크박스를 갱신하고, 아직 하지 않은 첫 단계부터 이어간다.
테스트가 진행 중이라면 **14일 연속 카운터가 끊기지 않았는지**(3절 E의 금지 항목)를 함께 확인한다.

**2026-08-25 시점의 답**: R3(vc4) 코드·AAB까지 끝났고 웹 배포와 Play 게시만 남았다.
그 다음 분기점은 **R4(vc5)**다 — 3절 E-2대로 Play Console `앱 요약(vitals)`에 실제로 잡힌
크래시·ANR이 있으면 그것을 고치고, 없으면 나침반 보정 안내(6번)·핀 렌더링 성능(7번)으로
채우거나 생략한다. R2·R3만으로 Doply 조건(2회 이상)은 이미 충족했으므로 R4는 여유분이다.
R4를 한다면 **8/28(금)로 당긴다** — 원래 계획일 8/29는 토요일이라 테스터 업데이트가 늦어진다.

## 6. 참고 링크

- Doply 한국어: <https://www.doply.io/ko>
- Google Play Console: <https://play.google.com/console/>
- Google 실제 Android 기기 인증: <https://support.google.com/googleplay/android-developer/answer/14316361?hl=ko>
- Google 비공개 테스트 설정: <https://support.google.com/googleplay/android-developer/answer/9845334?hl=ko>
- Google 신규 개인 계정 테스트 요건: <https://support.google.com/googleplay/android-developer/answer/14151465?hl=ko>
